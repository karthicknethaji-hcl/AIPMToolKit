# Changelog — AI PM Toolkit

## v9.17.02 — Requirement Agent bug/polish pass

- **Fixed the critical Finalize bug: Feature Canvas showed empty after Finalize.** Root cause: the finalize sequence wrote generated features into `capStore`'s `featStore` only, in a shape missing the `metric`/`stage`/`cap`/`selected` fields the normal generation path always attaches, and never pushed anything into `scCanvas` — the array Feature Canvas actually renders from (confirmed via `capability-canvas.js`'s `ccSendToStoryCanvas()`, the only existing capStore→scCanvas pipe). Fixed by normalizing generated features to the same shape `ccGenerateFeatures()` produces and replicating the scCanvas push (with `intakeBriefId`/`rqNumber` carried through, dedup by feature id, `fcUpdateTabBadge()`/`markTabPending('fc')` called to match).
- **Fixed the Finalize-blocked modal's unresolved-question count not matching the chat.** The model's `openQuestions` JSON array and the numbered questions it writes into `chatReply` were never required to correspond 1:1 — both prompts now mandate exact parity (same count, same order, never re-including an answered question), and a code-level dedupe (`_raDedupeQuestions()`) guards against a duplicate question entry inflating the count regardless.
- **Chat bubble spacing/alignment fixed:** `.ra-chat-body` was missing `display:flex;flex-direction:column;gap:14px` (present on Guided Launch's equivalent `.gl-chat-body`), so `.gl-msg-row`'s reused `align-self`/`gap` rules had no flex parent to act on — bubbles touched with no gap and user replies never right-aligned.
- **Left panel rebuilt onto the shared `.left`/`.ph`/`.collapse-btn` pattern** every other tab (and Guided Launch's own `#gl-left`) uses, instead of a bespoke 280px non-collapsible panel — restores the standard 300px width and adds the missing collapse/expand affordance. Also fixed the filter-chip active-state bug (`raSetFilter()` updated the conversation list but never re-rendered the chips themselves, so only "All" ever showed as selected).
- **Live Draft brought back to full brief detail** — restored Objectives & Success Criteria, Assumptions & Open Questions (persisted, distinct from the chat transcript), and Out of Scope & Risks sections (present in the pre-RA reference brief structure), plus the new Recommended Features section (per-capability preview of what Finalize will generate), across both the opening and every ongoing turn's prompt.
- **Added a Live Draft document version badge** (`v0.01`, `v0.02`, ... — `conv.draftVersion`, persisted), shown beside the title and frozen once finalized.
- Personalized the opening chat greeting ("Hi [FirstName], ...", via `currentUser.displayName`'s first token); removed an em dash from the assumption modal's body copy; fixed the modal's "Review Questions" button rendering at the generic 12px `.modal-cancel-btn` size instead of the 11px size used by the Warn CTA beside it (plus a full DESIGN_SYSTEM.md §8/§5 pass: Live Draft panel now follows the mandatory chrome-content-chrome header/body/footer background rule and standard 440px right-panel width).

## v9.17 — Requirement Agent module (global, release-scoped, multi-conversation)

- **New global Requirement Agent tab (`#tab-ra`, labeled "Requirement Agent (Release)")** — a multi-conversation, release-scoped requirements agent that sits between Capability Canvas and Feature Canvas: one conversation = one release scope, symmetric across every capability it touches from turn one. Distinct in mechanism from the pre-existing `gl-*`/`guided-launch.js` flow (whose tab copy was separately renamed to "Requirement Agent" in v9.16, and whose Settings module toggle is `appSettings.featRA`) — see this build's own report for the resulting three-way naming collision, flagged for a human product decision rather than resolved silently.
- **Capability Canvas gains a session-level RA toggle** (`raEnabled`, toolbar switch) — when on, a single "Define Requirements" CTA replaces the per-capability selection/bulk-generate bar, and the right feature panel drops its own CTA in favor of the finalized-release-brief flow.
- **Finalize sequence**: resolves open questions with agent-made assumptions (persisted, marked `**Assumed:**` in the live draft) when the PM proceeds past the Type-1 Warn "unanswered questions" modal, creates/finds `capStore` entries for any capability tagged "will be created", generates features for every touched capability from the finalized brief, assigns a sequential `RQ##` number via a new atomic `ra_next_seq` Postgres RPC (`sql/ra-requirement-agent.sql` — not yet run against Supabase), tags every generated feature `intakeBriefId`/`rqNumber`, and navigates to Feature Canvas automatically.
- **`buildPIStoryPrompt()` (prompts.js)** extended with two optional trailing params for future RA-sourced story generation; `piGetSelectedStories()`/`piGetAllStories()` (pi-planning.js) now carry `intakeBriefId`/`rqNumber` (and `piGetAllStories()` also gained `priority`/`origin`, a pre-existing unrelated omission fixed in the same pass).

## v9.16 — Requirement Agent settings module, Home consolidation, two bug fixes

- **Added Requirement Agent as a proper Settings-gated module** (default ON) — new Feature Modules row and master switch, reordered to lead the module list; renamed the stale "Metrics Definition" Settings label to "Metrics Dictionary" to match every other live reference to that module.
- **Consolidated Home's Quick Launch / Guided Launch buttons into a single "Launch Session" CTA**, routed by a new Requirement Agent toggle on the Home panel; removed the redundant "I already have a capability plan" shortcut (Capability Canvas's own dual-entry screen already offers this). Moved Additional Context to the end of the panel and dropped the divider line between toggle rows.
- **Renamed the "Guided Launch" tab and its user-facing copy to "Requirement Agent"** (tab label, chat header, new-session stage label, session-card pill) — internal identifiers (`glStatus`, `gl-*` element ids, filenames) are unchanged. Existing sessions created before this release keep showing "Guided Launch" on their session card, by design — no backfill.
- **Fixed an unwanted horizontal scrollbar on Feature Canvas's card grid** — `#sc-cards-scroll` was missing `overflow-x:hidden`.
- **Fixed Guided Launch's chat input and send button staying visually enabled after finalize** — they were already functionally blocked, just not visually disabled to match.
- Server-side: renamed the `guided-launch` caller/tier-lookup key to `requirement-agent` (logging only — no change to historical usage data).

## v9.15.04 — Guided Launch: polish pass on the v9.15.03 fixes

- **Narrowed the leave-confirmation modal to mid-response only** — it now warns only when leaving while the agent is actively responding, not on every departure from an unfinalized chat (reversing v9.15.03's original "any departure" design, per explicit product decision).
- **Fixed the Discovery Map tab button not showing after resuming an interrupted-generation session** — the shared "Generation was interrupted" state now explicitly reveals `tab-mm`, since the normal reveal condition (a populated Discovery Map) is correctly false at that point.
- **Fixed Guided Launch's left panel spacing** to match Discovery Map's real left panel — it was missing the `display:flex`/`gap` treatment that actually produces the spacing between rows, not just the padding value.
- **Hid the footer note entirely once finalized** (was only re-texted before), which also fixes a footer height mismatch across the three panels.
- **Added a version number to the requirements brief title** (e.g. "Zomato Requirements Brief v0.01"), incrementing on every successful revision.

## v9.15.03 — Guided Launch: context bug fixes, leave-confirmation, resume-to-DM link

- **Fixed a real data bug:** the opening chat prompt read the wrong field names on the product/company profile (`name`/`description` instead of the real `productName`/`productDesc`/`companyName`/`companyStrategy`), so every fresh Guided Launch session opened as if no product profile existed at all, regardless of what was actually configured in Settings.
- **Fixed a second, related data bug:** `glCreateAndOpen()`'s parameter was named `sessionContext`, shadowing the global variable of the same name for its whole function body — so the global was never assigned at creation, and every snapshot saved during the chat (including the very first one) persisted a stale or null `sessionContext`, even though the live in-memory copy driving the chat stayed correct. This surfaced as an empty left panel on resume, since resume reads the persisted snapshot back. Fixed by renaming the parameter and explicitly assigning the global, matching how Quick Launch already does it.
- **Added a leave-confirmation modal** for an unfinalized Guided Launch session, reusing the app's existing confirmation-modal component (same one Discovery Map's in-progress-generation guard uses) with Guided-Launch-specific copy, since no progress is ever actually lost.
- **Fixed the upload control staying enabled while the agent is responding** (chat input and send were already correctly disabled); added a "Continue to Discovery Map" link, shown only once finalized, for the case where a user finalizes then leaves before generation ever completes.

## v9.15.02 — Guided Launch: unified onto mt_sessions

- **Architectural fix:** a Guided Launch session is now a real `mt_sessions` row from the moment of creation — the same `sessionStoreCreate()`/`sessionStoreSave()`/`sessionStoreRestore()` machinery every other canvas uses — instead of a separate `mt_intake_sessions`/`mt_intake_messages` pair that only became a real session at Finalize (via a second, unrelated row). Those two tables are dropped (migration run directly, no data migrated — test data only). Chat transcript and draft/final brief now live in the session's own `snapshot.gl*` keys; a new `mt_sessions.intake_status` column (`active`/`completed`/`null`) drives Guided Launch's tab visibility and resume routing.
- **Fixes four real product-feedback items this unification was the actual root cause of:** the bolted-on "Continue a Guided Launch session" banner on Home is gone (sessions now show as normal cards); the left panel shows real Company/Product/Type/Industry content via the same function Discovery Map's own panel uses, not a thin lookalike; the Guided Launch tab no longer shows with no active session; and resuming a finalized (Discovery-Map-generated) session that originated from Guided Launch now still shows that tab, so the original chat/brief is never permanently orphaned.
- **Two additional corrections from the same feedback round:** the collapsed-panel reopen chevron was pointing the wrong direction (re-derived from raw coordinates this time, not re-asserted from the prior cycle's comment); chat bubble colors reverted from v9.15.01's overcorrection (agent back to gray, user to blue).

## v9.15.01 — Guided Launch bug-fix and polish pass (26 items)

- **Fixed a real data-loss bug (highest severity):** the `authenticated` role was never granted `EXECUTE` on `is_active_company_member(uuid, uuid)`, so every write to `mt_intake_sessions`/`mt_intake_messages` failed silently since v9.15 (fixed via a direct Supabase grant, not app code); separately, abandoning an unfinalized Guided Launch session and resuming a different one left its in-memory state, rendered chat, and tab visibility stuck behind — `homeClearSession()` now resets all of it via a new `glResetState()`. Also fixed: left panel rebuilt on the app's real product-context pattern with a working collapse control, "Reopen requirements draft" button removed as redundant with the collapsed rail, right-panel collapse chevron direction corrected, header/footer padding aligned across all three panels, `diag-action-bar` no longer leaks into the tab from Discovery Map, and the Guided Launch tab moved next to Home in the tab row.
- **Fixed the "Thinking..." indicator** — was rendering an orphaned Capability Canvas spinner class that doesn't exist in this file; replaced with a self-contained pulsing-dots indicator and rotating status phrases, no borrowed classes.
- **Fixed chat cosmetics:** user avatar showed a hardcoded "You" instead of real initials, agent/user bubble colors were reversed from the intended pairing, the "Live Draft" eyebrow was gray instead of purple, revising a section no longer scrolls it into view, and the upload control stayed visually clickable after finalize.
- This is a bug-fix/polish pass on the v9.15 feature, compressed into one bullet group per the project's patch-entry format — see PROJECT_MAP.md/session notes for the full 26-item breakdown.

## v9.15 — Guided Launch: conversational intake flow

- **Added — Guided Launch**, a second Home entry path alongside Quick Launch: a new `gl` tab with a conversational chat panel (left: existing product context, center: chat, right: a live, read-only Markdown requirements brief). The agent opens with a summary of the same context Quick Launch would use (company/product profile, session docs) plus a first-draft brief; disagreement in chat produces a targeted, flash-highlighted revision rather than a full rewrite; mid-chat document upload gets extracted/summarized into the draft, never stored raw. New tables `mt_intake_sessions`/`mt_intake_messages` (migration run separately).
- **Added — Finalize & Generate**, a single one-way action: locks the brief (`status: 'completed'`), then hands off to the existing, unmodified Discovery Map generation pipeline via `sessionContext.additionalContext` — no changes to the generation prompts themselves.
- **Changed — `callAPI()` gains an optional `extraFields` param** (`scripts/api.js`) and `proxy/server.js`'s `mt_ai_usage_events` insert now tags Guided Launch calls with `session_type: 'ChatCanvas'`, since its session id points at `mt_intake_sessions` rather than `mt_sessions` and can't go through the normal product-id lookup. Model resolution uses the standard Optimized/`resolveModelDecision()` chain (general tier), not a hardcoded model string.
- **Changed — `home.js`**: `_homeDoLaunch()`'s sessionContext-building logic is now shared via `_homeBuildSessionContext()` so both launch paths read identical context; `_homeUpdateLaunchBtn()` and `homeSessionResume()` gained additive branches for the second button and for routing an active intake session back into Guided Launch instead of the Discovery Map.

## v9.14.05 — Fix Home "AI Recommendations" empty since v9.14

- **Fixed — `_homeCallAIRecs()` (`scripts/home.js`) does its own direct `fetch()` to the proxy rather than going through `api.js`'s `callAPI()`, and its response handler still parsed `data.content[0].text` (the pre-v9.14 Anthropic-native envelope), while the proxy has sent a provider-neutral `{text:...}` envelope for every provider since v9.14.** This silently produced `raw = '[]'` via the line's own fallback, parsed to an empty array, and rendered "No recommendations available" with no visible error, regardless of which provider was selected. Now reads `data.text || '[]'`, matching `callAPI()`'s existing v9.14 pattern. Confirmed no other line in `_homeCallAIRecs()`/`_homeRenderAIRecs()` assumes the old `data.content` shape, and confirmed (via grep of every other direct-`fetch()` caller outside `api.js`) that no other call site has this same bypass-and-stale-parse issue — `auth.js` and `team-management.js`'s direct fetches hit unrelated, non-AI-content endpoints.

## v9.14.04 — Fix Gemini resolved-model field name

- **Fixed — the Gemini adapter's `normalizeSuccess()` read `data.modelVersion` for `resolvedModel`, a field that doesn't exist in any real Gemini Interactions API response** (every example response in the raw doc paste — interaction-created, interaction-completed, get-interaction — uses the top-level field `model`, not `modelVersion`). As written, `resolvedModel` would have silently fallen back to `null` on every successful Gemini call, meaning the dated-alias-drift tracking this field exists for (same purpose as Anthropic's `response_model` handling) would never actually populate. Now reads `data.model`, matching the confirmed response shape.

## v9.14.03 — Gemini added as a third AI provider

**Versioning note:** this lands as a patch version (v9.14.03) rather than a new feature release (v9.15) per an explicit project-owner call — Gemini is scoped as completing the same LLM-agnostic umbrella of work as the rest of v9.14.x, not a separate feature. That's a deliberate override of this project's usual feature-vs-patch distinction, recorded here so the choice reads as intentional rather than a misapplication of the versioning rule. The entry below is written to the full substance of what changed, not compressed to a single bullet just because of the version-number slot it landed in.

- **Google Gemini added as a full third provider**, alongside Anthropic and OpenAI: new entry in `_spProviders`/Settings' AI Provider dropdown, a new `geminiAdapter` in `proxy/providerAdapters.js` (Interactions API), and Gemini added throughout the provider-generic plumbing (`TIER_MODEL_BY_PROVIDER`, `_MODEL_FALLBACK_BY_PROVIDER`, `MODEL_CATALOG_BY_PROVIDER`, `ORG_API_KEY_BY_PROVIDER` / `GEMINI_API_KEY` env var). This was previously deferred (Section 8 of `multi-llm-provider-spec-DRAFT.md`) pending an apparent GA-vs-Beta contradiction in Google's docs — on direct re-examination of the exact quoted passages, that turned out to be a mis-read, not a real inconsistency (one passage describes product maturity, the other describes URL versioning scheme — two different things). Resolved, not worked around.
- **Confirmed real model IDs, via direct raw-documentation paste** (a stronger source than the search-tool passes used for OpenAI's literals in v9.14.02): `gemini-3.5-flash-lite` (lightweight tier), `gemini-3.6-flash` (general tier and fallback). **No premium tier** — `gemini-3.1-pro-preview` is the only Pro-class model and carries "preview" directly in its model ID string, confirmed independently twice as not a stable option; `TIER_MODEL_BY_PROVIDER.gemini.premium` is `null` by design, and the existing null-tier fallthrough in `resolveModelDecision()` (already shipped, verified correct) handles it without further change.
- **Endpoint, field names, and response shape confirmed via the same direct-documentation source:** `POST https://generativelanguage.googleapis.com/v1beta/interactions`; `system_instruction` as a top-level string; `input` accepting the same `{role, content}` array shape already used for the other two providers; response text extracted from a typed `steps[]` array (filtered for `type=="model_output"`, then that step's `content[]` filtered for `type=="text"` — never a fixed index, same defensive pattern as the OpenAI adapter). One caveat carried at a visibly lower confidence tier: the `x-goog-api-key` auth header came from a search/fetch-tool pass, not a direct human paste like the rest of this section — reasonably corroborated (its own example target matched the already-confirmed endpoint) but worth a quick human glance at `ai.google.dev/gemini-api/docs/api-key` before treating it as fully closed.
- ~~**Not yet done:** `mt_model_pricing` rows for the two Gemini models~~ — **DONE (2026-07-25, no app code change, DB-only):** rows for `gemini-3.5-flash-lite` and `gemini-3.6-flash` inserted directly against `pgt-dev` by Nethaji, following the direct-confirmation pricing process and the existing Anthropic rows' `effective_from`/`effective_to` pattern.

## v9.14.02 — OpenAI model IDs now live (Phase 1)

- **Replaced the `[VERIFY_*]` OpenAI model placeholders with real model IDs** (`_spModelsByProvider`, `TIER_MODEL_BY_PROVIDER`, `_MODEL_FALLBACK_BY_PROVIDER`, proxy `MODEL_CATALOG_BY_PROVIDER`): `gpt-5.6-luna` (lightweight), `gpt-5.6-terra` (general/fallback), `gpt-5.6-sol` (premium) — confirmed 2026-07-25 via direct human screenshot of `developers.openai.com/api/docs/models`, not a search/fetch-tool result, per this project's standing distrust of tool-mediated results on this exact question. **Residual uncertainty, stated plainly:** independent press reports this GPT-5.6 family as limited-preview to ~20 organizations; this has NOT been confirmed callable by this org's account via a successful live call (blocked on a $0 billing balance) — if generation fails with a permission/model-unavailable error, check org access/billing first. `mt_model_pricing` rows for these three models were inserted directly against `pgt-dev` by Nethaji shortly after this release (2026-07-24, no app code change, DB-only), following the existing Anthropic rows' `effective_from`/`effective_to` versioning pattern.

## v9.14.01 — Fix misleading AI Model helper text in Settings

- **Fixed — the "AI Model" helper text in Settings claimed an unrecognized/unavailable model automatically falls back to "the next tier down," which the app has never done** (`resolveModelDecision()` and the proxy's `isKnownModel()` fail fast with a clear error instead — confirmed via grep, no downgrade logic exists anywhere). Copy now reads "generation fails with a clear error rather than silently switching models," matching actual behavior.

## v9.14 — Multi-LLM provider support, Phase 1 (Anthropic + OpenAI)

- **Added — AI Provider selection in Settings** (Company Profile & Access section): admins can now switch which AI service the company's calls route through. "Optimized (Default)" mode resolves to a smart per-caller tier within whichever provider is active, not a hardcoded Anthropic model string. Gemini deliberately deferred — see the Section 8 open question in `multi-llm-provider-spec-DRAFT.md` about the `/v1beta/interactions` GA-vs-Beta documentation contradiction.
- **Added — `proxy/providerAdapters.js`**, a new canonical, shared adapter module (Anthropic + OpenAI) imported by both proxy runtimes (`proxy/server.js`, `netlify/functions/anthropic-proxy.js`) rather than hand-duplicated. Each adapter normalizes success/HTTP-error/transport-error into a common shape; the proxy validates the resolved model against this module's catalog before forwarding upstream (fail-fast).
- **Changed — proxy resolves the company's provider server-side**, never from the client-sent `provider` field (diagnostics-only) — reads `mt_company_settings.settings.provider` directly, alongside the existing company-membership check. `mt_ai_usage_events.provider` always reflects this server-resolved value.
- **Changed — response envelope is now provider-neutral** (`{text: ...}`), replacing a direct pass-through of Anthropic's `{content:[{text}]}` shape. `scripts/api.js`'s `callAPI()` updated accordingly.
- **Changed — `_byokKey()` (scripts/auth.js) is now provider-scoped in addition to its existing company-scoping** (`hcl_ak_<companyId>_<provider>`, was `hcl_ak_<companyId>`). A new one-time migration (`_migrateByokKeyToProviderScopeIfNeeded()`) carries forward any already-saved Anthropic key into the new slot on first load, alongside the existing pre-v8.105 migration.
- **Added — retry-once policy for transient upstream errors only** (rate-limit/overload/5xx), never for invalid-key/permission/malformed-request errors and never for transport-level failures/timeouts (an ambiguous already-may-have-billed outcome, not safely retryable).
- **Not yet production-ready:** OpenAI's model IDs, prices, key-prefix pattern, and Responses API field names are `[VERIFY]`-tagged placeholders pending independent confirmation against `platform.openai.com/docs` — see `multi-llm-provider-spec-DRAFT.md` Section 7. The provider-agnostic scaffolding (this release) ships independently of those literals.
- **Not yet done — `mt_model_pricing` rows for OpenAI models.** Blocked on the same Section 7 verification; live schema for that table was confirmed against `pgt-dev` (versioned pricing history, not a flat sheet — `effective_from`/`effective_to` columns) but no INSERT has been written.

## v9.13.04 — 2026-07-23

- **Fixed — four more "KPI Tree" strings survived v9.13.03 because that pass only grepped `.js` files, never `.html`**: `index.html` had a static "KPI tree" legend item in the Feature Canvas origin key (the one visible in the screenshot that triggered this patch), plus the matching Feature Canvas filter-dropdown checkbox, the custom value-chain hint text, and the Market Intelligence toggle subtext. All four now read "Discovery Map"; a full-repo `grep -rl "KPI Tree" /mnt/project/` (no extension filter) confirms the only remaining hits are code/CSS comments and internal dev docs (DESIGN_SYSTEM.md, PROJECT_MAP.md, CHANGELOG.md history) with no end-user visibility.

## v9.13.03 — 2026-07-23

- **Fixed — the v9.13.02 "KPI Tree" → "Discovery Map" rename missed 22 user-visible strings**, concentrated in Capability Canvas (9) and Feature Canvas (5), plus scattered instances in Diagnostic View, the Discovery Map generation flow, Market Intelligence, Settings module descriptions, and the DOCX export origin-label function. Root cause: the prior pass built off a partial catalogue rather than a full-repo grep run as a final gate before packaging. This patch is the result of an exhaustive `grep -rn "KPI Tree"` across every file, with each hit classified as user-visible or code-comment; all 22 user-visible instances are now "Discovery Map" (loader/progress messages, one in-flight generation guard string, and two pre-existing module descriptions were left unchanged per explicit instruction — code comments were left as-is since they carry no user impact).

## v9.13.02 — 2026-07-23

- **Fixed — PI planning could leave stories in backlog despite available capacity**: Stories with unassigned blocking dependencies were permanently disqualified from all sprints, even those with free capacity. The readiness check now treats unassigned blockers as non-blocking until they themselves are assigned, allowing dependent stories to place once their blockers eventually schedule.
- **Renamed — "KPI Tree" terminology replaced with "Discovery Map" umbrella term** across UI, exports, and reports for consistency. Settings control relabeled to "Outcome Metrics Depth" (specific to that mode). Session mode labels "Outcome Metrics" and "Process Area" unchanged.

## v9.13.01 — Fix null product_id in AI usage-tracking events

- **Fixed — every AI usage-tracking event (`mt_ai_usage_events`) recorded `product_id` as `NULL`, even for normal generation calls with a genuinely active product.** Root cause: `product_id` was read from the client's `activeProfileId` (a Home-tab-scoped UI variable), which is not reliably populated once generation is running deep inside an already-active session. Fixed by adding a real `product_id` column to `mt_sessions` (backfilled from existing `product_name` matches, then enforced `NOT NULL` — a session is always launched against exactly one product) and deriving `product_id` server-side in `proxy/server.js` via `session_id -> mt_sessions.product_id`, which is now authoritative whenever a session exists. `session-store.js` and `live-sync.js` updated to capture and sync the new column; existing `mt_ai_usage_events` rows (all showing the bug) were truncated rather than backfilled, since the client-reported value they'd be backfilled from was exactly the unreliable data this patch fixes.

## v9.13 — AI usage-tracking capture layer

- **Added — every AI proxy call is now logged to a new `mt_ai_usage_events` table**, capturing company/product/user/role, caller, requested and actual model, full token usage (including split 5-minute/1-hour prompt-cache tokens), request/response byte sizes, duration, and success/error/timeout status with error detail — the foundation for a future usage/cost dashboard. Insert is awaited (not fire-and-forget) from `proxy/server.js`, on both the success and error/timeout paths, and never blocks or fails the user's actual AI response if the telemetry write itself fails.
- **Added — `mt_model_pricing` table** for point-in-time-correct cost calculation at query time (never stored on the event row itself, so historical costs stay correct even after future price changes). Seeded with Sonnet 4.6 / Haiku 4.5 rates, backdated to 2026-04-01 to cover the project's full build history to date.
- **Added — model-selection provenance tracking**: every usage event records not just which model ran, but *why* — `optimized_caller_default`, `user_selected_model`, `batch_threshold_override`, or `explicit_override_unclassified` (an honest "not yet audited" bucket, never a silent mislabel). Enables verifying whether the app's "Optimized" default is actually being used as intended, versus users overriding it. Required a new `resolveModelDecision()` in `api.js`; `resolveModel()` remains a thin wrapper over it so no existing call site needed to change.
- **Schema designed forward-compatible for future OpenAI/Gemini support** (`provider` column, provider-agnostic raw usage JSONB capture) — no actual multi-provider code shipped in this release, schema only.

## v9.12.06 — Prototype Canvas wireframe sandbox hardening; Outcome Pulse decimal display fix

**Note:** bundles two unrelated fixes by request, not one shared root cause.

- **Fixed/hardened — the visible wireframe preview iframe received raw, unsanitized AI-generated HTML**, causing a "Blocked script execution... sandboxed" console message on every prototype view whenever the model's output happened to include a `<script>` tag (cosmetic — the wireframe always rendered correctly regardless, but this would be flagged by QA as an error). Root-caused via a ChatGPT adversarial review, which also surfaced a materially more serious, pre-existing gap: **the separate hidden capture iframe (used for PNG export) had no sandbox attribute at all**, relying entirely on a small blocklist sanitizer with a fail-open fallback — a sanitizer bypass there could have resulted in full same-origin script execution, unlike the visible preview, which was always at least sandboxed. This patch: (1) applies the same sanitization to the visible preview before it's ever rendered, so there's nothing left for the sandbox to block; (2) fixes the sanitizer's fail-open fallback to fail closed instead (shows nothing rather than partially-filtered content on a parse failure); (3) adds `sandbox="allow-same-origin"` to the previously-unsandboxed capture iframe, blocking script execution there too while preserving the same-origin access it needs for html2canvas; (4) tightens the visible preview's sandbox from `allow-same-origin` to a fully empty sandbox (opaque origin), since nothing in the app reads into that iframe's document; (5) hardens the shared sanitizer to also strip `<meta refresh>`, `<base>`, `formaction`/`action` javascript: URLs, and control-character-obfuscated `javascript:` schemes; (6) fixes a blob-URL lifecycle bug where the previous URL was revoked before the replacement was confirmed successful, which could leave a variant's stored URL pointing at an already-revoked blob on a failed retry.
- **Fixed — Outcome Pulse's baseline/target/actual values with exactly one decimal digit displayed unpadded** (`61.5` instead of `61.50`), while whole numbers and two-decimal values already displayed correctly. `opFormatNumber()` now rounds to 2dp and pads only when the result isn't a whole number, so `100` stays `100`, `61.5` becomes `61.50`, and `61.5678` still correctly rounds to `61.57`.

## v9.12.05 — Occupancy-lock logging cleanup; Suggest Experiment model resolution fix

**Note:** this patch bundles three unrelated fixes by explicit request, not one shared root cause — flagging per this project's own patch-entry convention rather than forcing an artificial single bullet.

- **Fixed — `sessionStoreSave()`'s defense-in-depth guard logged a red `console.error` every time a view-only user (including one correctly demoted by the Session Occupancy Lock) exited a shared session** via `homeClearSession()`'s own save-on-exit attempt — a normal, expected outcome, not a genuine error. Added an optional `expectedBlock` parameter, defaulting to unset for all ~93 other call sites (unchanged, still `console.error` — a blocked save from any of those genuinely can indicate a missed per-screen permission gate, and that signal must not be silenced). Only `homeClearSession()`'s own save call now passes `true`, downgrading to a quiet `console.log` for that one specific, known-benign case.
- **Removed temporary diagnostic logging** (`[DIAG occupancy]`, `[DIAG resume]`) added across the v9.12.02–v9.12.04 debugging thread, now that the underlying toast/occupancy bugs are confirmed fixed.
- **Fixed — Suggest Experiment (Outcome Pulse) always called `claude-haiku-4-5-20251001` regardless of the "Optimized (Default)" or an explicit model choice in Settings:** the call site hardcoded a model string directly, completely bypassing `resolveModel()`'s precedence chain. Registered `outcome-pulse-suggest` in `CALLER_MODEL_DEFAULTS` (Sonnet 4.6 for Optimized, per confirmed intent) and removed the hardcoded override — an explicit user model choice is now also correctly respected here, same as every other AI caller in the app. Note: this is a real behavior change for anyone on Optimized — Suggest Experiment now uses Sonnet 4.6 instead of Haiku, with the corresponding cost/latency tradeoff.

## v9.12.04 — Session Occupancy Lock toast was being silently overwritten by "Session restored."

**Fixed — the Session Occupancy Lock's read-only/demotion toast never displayed, even though the underlying access control was working correctly (confirmed via diagnostic logging and the pre-existing `sessionStoreSave` view-only block on exit):** `showToast()` uses a single shared DOM slot with no queue — a later call always overwrites an earlier one's content, even within the same synchronous tick. `sessionStoreRestore()`'s unconditional `showToast('Session restored.', 'info')` near the end of the function was silently clobbering the occupancy lock's own warning toast, which fires earlier in the same restore. A flag now suppresses the generic "Session restored." message specifically when the occupancy lock already claimed the toast slot with its more important, more specific message for that restore.

## v9.12.03 — Occupancy-gate diagnostic logging restored (no logic change)

**Fixed — the v9.12.02 rewrite of `sessionStoreRestore()` accidentally dropped the occupancy-gate diagnostic logging** (entry/skip reasons, claim result) that had been in place to root-cause the original toast/edit-access bug — a genuine reproduction on v9.12.02 showed the re-entrancy fix behaving correctly (no duplicate call) but the underlying issue still occurring, with no visibility into why. Logging restored, no other behavior changed.

## v9.12.02 — Session Occupancy Lock toast/demotion silently lost on duplicate session-open

**Fixed — a rapid duplicate trigger on the same session card (e.g. a double-click) silently discarded the v9.12 Session Occupancy Lock's toast and read-only demotion, leaving a blocked user with full edit access until their first save was rejected server-side:** `sessionStoreRestore()` had no re-entrancy guard, so a second call for the same session invalidated the first call's in-flight occupancy claim via the existing stale-supersede check — correct behavior for a genuine switch to a *different* session, but wrong for a duplicate call on the *same* one. Root-caused via live console diagnostics (a claim result of `{claimed:false, reason:"held"}` was logged, but execution silently returned before the toast/demotion code ran). Fix, all directly related to the same root cause: (1) a same-session-only re-entrancy guard, which deliberately still allows a legitimate rapid switch to a *different* session to supersede an in-flight restore, unchanged from prior behavior; (2) `_ssRestoring`'s cleanup is now ownership-checked (only the call that still owns the current restore sequence clears it), fixing a related pre-existing bug where a stale call waking up late could incorrectly clear a newer, still-active restore's flag; (3) the occupancy claim call now fails closed on a thrown/rejected RPC (network or auth failure), demoting to read-only with an explanatory toast rather than silently leaving the session editable with no occupancy actually held; (4) `homeAIRecClick()`'s fixed 50ms timer before switching tabs — now provably unreliable against a restore chain that can include the occupancy claim's own round-trip — now awaits the restore directly instead of guessing a delay. Temporary diagnostic logging remains in place in this build to confirm the fix in the field; will be removed in a follow-up patch once verified.

## v9.12.01 — Outcome Pulse tab restored; temporary occupancy-gate diagnostics added

**Fixed — the Outcome Pulse tab rendered completely blank with a console error (`Uncaught SyntaxError: Unexpected token '<'`) on every session:** `scripts/outcome-pulse.js` was accidentally omitted when v9.12 was packaged, so the browser received a 404/HTML fallback in place of the script and failed to parse it as JavaScript. The file is restored to the build. (Temporary `console.log` diagnostics were also added to the v9.12 Session Occupancy Lock gate in `session-store.js`, to be removed once the reported "occupancy check not blocking a second editor" issue is root-caused — not a behavior change, logging only.)

## v9.12 — Session Occupancy Lock ("Single User Editing" mode)

**Added — a new Collaborative Editing setting (Settings → Output Depth → Session Sharing)**, replacing the old two-way "View only / Collaborative editing" radio choice with a single on/off toggle plus, once on, a choice between **Single User Editing** (new, default) and **Multi User Editing** (today's existing unrestricted concurrent-edit behavior, fully unchanged, same amber coordination warning as before).

**Added — Session Occupancy Lock**, the new mechanism behind Single User Editing: when a shared session's Collaborative Editing mode is Single, opening the session now attempts to claim exclusive edit occupancy via a new `claim_session_occupancy` RPC. If someone else currently holds it, the opener is shown a toast naming the current editor and is placed into read-only viewing for that session instance — no live promotion once the other person leaves; the standard way back into edit mode is leaving and reopening the session, matching this feature's deliberately simple design. Backed by three new `SECURITY DEFINER` RPCs (`claim_session_occupancy`, `release_session_occupancy`, `heartbeat_session_occupancy`) and three new nullable columns on `mt_sessions` (`occupant_user_id`, `occupant_at`, `occupant_user_name`), kept entirely separate from the pre-existing generation-lock columns/RPCs (`active_user_id`/`active_at`/`acquire_generation_lock`) since those are already generation-specific elsewhere in this app (the admin "remove team member" flow clears them explicitly as such) — conflating the two would have been incorrect, not simplifying.

Occupancy is held via a 22-second heartbeat, independent of and structurally separate from the existing generation-lock heartbeat — a stalled/expired lease (60-second staleness window, matching the generation lock's own window) is always reclaimable by a new opener regardless of what happened to the previous holder's tab. Released cleanly on normal navigation away from a session (`homeClearSession()`), with a best-effort attempt on tab close; the staleness window is the real fallback for a crashed or force-closed tab either way. Team member removal now also clears any occupancy lock they held, alongside the existing generation-lock cleanup for the same event.

**Known, deliberately accepted limitation:** the same signed-in user in two different browser tabs/windows can both hold occupancy on the same session simultaneously — the claim logic's own-user-reentrant check permits this by design, mirroring an identical, already-accepted gap in the existing generation lock (documented there as not solving cross-device/cross-tab same-user concurrency). Not addressed in this release; revisit if it proves to matter in practice.

## v9.11.05 — Outcome Pulse on by default; Experiment Library polish

**Changed — Outcome Pulse is now on by default** for new installs and any admin who hasn't explicitly toggled it, rather than requiring an opt-in from Settings.

**Fixed — Experiment Library's empty state sat near the top of the panel instead of centered in the available space,** and showed a redundant "Experiments" heading above a message that already explained there was nothing to show. Now centers both horizontally and vertically, with the heading suppressed when the panel is empty.

**Added — two more rotating messages while an experiment suggestion is being generated,** touching on comparing against the feature's hypothesis and looking for an angle that could move the metric, alongside the existing message. The rotation is now guaranteed to stop on every exit path (success, no-recommendation, a stale/abandoned attempt, or an error), matching the same cleanup discipline already used elsewhere in this app's other rotating-message loaders.

## v9.11.04 — Outcome Pulse experiment-derived features now show the correct tracked metric; Log Result signal defaults sensibly

**Fixed — an experiment-derived feature's own tracked metric still didn't match the original feature it came from, even after v9.11.03:** the previous patch correctly gave the new card the original's real KPI-tree grouping metric, but a feature's own hypothesis metric is intentionally allowed to be more specific than that (by original design — e.g. a feature grouped under "Organic Sign-ups" can track "Share-to-Invite Conversion Rate" as its own hypothesis). The new sibling card's hypothesis now correctly inherits that more specific label from the original feature, which also fixes Experiment Library showing "No Outcome Pulse experiments suggested yet" for a metric that genuinely had some — the lookup was comparing against the wrong string.

**Fixed — entering an actual result without picking a signal pill left the row showing "Awaiting," indistinguishable from never having logged a result at all:** Log Result now falls back to the same suggested signal already computed and shown live next to the actual field, when no pill was explicitly clicked. An explicit pill click always takes priority.

**Fixed — the disabled "Suggest Experiment" menu item on experiment-derived cards showed no tooltip explaining why:** the mechanism used to visually disable it also silently blocked the tooltip from ever appearing on hover. It now shows normally.

**Added — a "Suggest Experiment" shortcut in Experiment Library's empty state,** so a PM doesn't need to close the panel and hunt for the row's menu again. Omitted when Library was opened from an experiment-derived card, matching that card's own disabled state.

## v9.11.03 — Outcome Pulse-generated experiment features now correctly grouped, seeded, and bounded

**Fixed — an Outcome Pulse-generated experiment sent to Feature Canvas still formed a second, duplicate stage section even after v9.11.02's fix:** the previous patch correctly captured the originating feature's capability and stage, but Feature Canvas actually groups cards by stage *and metric* together — the metric half was still coming from the AI's own restated hypothesis label rather than the real KPI-tree metric string. The originating feature's real metric is now captured and used for both the new card's grouping and its hypothesis label, so it lands as a true sibling under the exact same Value Chain Stage → Outcome Metric → Capability hierarchy the original feature already lives in.

**Fixed — the new experiment-derived feature's baseline and target showed as blank dashes instead of a usable starting point:** it now inherits from the original feature — baseline from the original's already-logged actual result if one exists, otherwise its baseline; target from the new experiment's own proposed number if the AI supplied one, otherwise the original's target.

**Fixed — an experiment-derived feature displayed the same "generate more experiments" badge and Suggest Experiment option as the feature it came from, creating a recursive loop:** experiments can only be generated from original features going forward. Experiment-derived cards show "Suggest Experiment" in a disabled state with an explanatory tooltip rather than hiding it outright, and no longer show the experiment-count badge. Experiment Library remains available on these cards, since it's read-only and gives a PM the full history of what's been tried for that metric without needing to find the original feature first.

## v9.11.02 — Outcome Pulse-generated experiments now correctly placed under their real hierarchy

**Fixed — an Outcome Pulse-generated experiment sent to Feature Canvas still landed in the wrong capability and stage** (the previous patch, v9.11.01, only corrected the metric-name string used for stage lookup, but never addressed the deeper cause): the send-to-Feature-Canvas step was reusing a mechanism built for real Diagnostic Analysis experiments, which genuinely have no originating feature and so need a synthetic "Diagnostic Experiments — {metric}" capability invented for them. Outcome Pulse experiments always have a real, known originating feature at generation time, so that mechanism never applied to them in the first place. The originating feature's actual capability and stage are now captured at generation time and used directly — no new capability is created, and the AI's own unconstrained stage-name guess (which could produce a metric-shaped, unbounded-length "stage" and caused a related horizontal-scroll layout issue on Feature Canvas) is no longer consulted for this origin. Real Diagnostic Analysis experiments are unaffected — that mechanism remains correct and unchanged for the case it was built for.

## v9.11.01 — Outcome Pulse Iteration Loop feedback fixes (first round of live testing)

**Fixed — a sent Outcome Pulse experiment never stayed selected on arrival at Experiment Canvas, and generating a second experiment made the first appear to vanish:** both traced to the same call, `laSelectRun(_runId)`, which unconditionally clears the selection Set (correct for a normal user-driven run switch, wrong here) and scopes the visible-experiments list to only the newly created run (hiding every other run's experiments from the current view, though still recoverable via the "All Experiments" nav item). Replaced with a shared navigation helper that leaves the view scoped to "All Experiments" and pre-selects only the new experiment's row — verified `laOpenDetailPanel()` has no dependency on the active-run scope, so nothing was lost by not narrowing it.

**Fixed — AI-generated experiments could land in the wrong stage bucket on Feature Canvas** (reproduced live: an Acquisition-stage feature's generated experiment was bucketed under Activation): the generation prompt let the AI freely restate the metric name, and stage resolution requires an exact string match against the live KPI tree — any paraphrase silently broke the match and fell back to the AI's own less-reliable stage guess. The metric identity (and its sibling success-metric fields) is now force-corrected to the feature's own known-correct metric string immediately after generation, not left to the AI's discretion.

**Fixed — features created from a sent experiment (either Outcome Pulse or a real Diagnostic Analysis run) never carried an outcome hypothesis, even though the source experiment already had one defined:** the send-to-Feature-Canvas step now maps the experiment's own success-metric and hypothesis fields into a real `outcomeHypothesis`, computing direction the same way every other hypothesis-creation path in this app already does.

**Added — Experiment Library cards are now clickable,** navigating straight to that experiment's detail panel in Experiment Canvas.

**Fixed — Experiment Canvas's detail panel always labeled an experiment's origin as "Product leak diagnostic," even for one generated through Outcome Pulse:** now reflects the actual source.

## v9.11 — Outcome Pulse Iteration Loop; Product Diagnostics renamed to Experiment Canvas

**Added — "Suggest Experiment" on any tracked hypothesis in Outcome Breakdown** (replaces the "Log Result" text link with a 3-dot menu holding Log Result, Suggest Experiment, and Experiment Library): generates one new experiment idea for that metric, informed by the feature's own hypothesis, its shipped stories, and every experiment already tried against that metric (real diagnostic runs and prior Outcome Pulse suggestions alike) so it doesn't propose a duplicate. If nothing new can be found, says so plainly and offers a refinement box rather than forcing a weak suggestion. Accepting sends the experiment straight into the existing Experiment Canvas pipeline — no new architecture for experiments themselves — and opens Experiment Canvas's own detail panel on it immediately.

**Added — Experiment Library, a read-only panel per metric** (opened from the same 3-dot menu) listing every Outcome Pulse-suggested experiment for that metric, with a live status chip (in Experiment Canvas / in Feature Canvas with no stories / in Feature Canvas with N stories) that always reflects current Feature Canvas state, including correctly reverting if a card is later deleted. Outcome Breakdown also gained a metric filter (alongside the existing signal filter) and a passive experiment-count badge per row.

**Changed — "Product Diagnostics" renamed to "Experiment Canvas"** throughout the app (tab label, Settings, panel headers, export button, FC card provenance tags, KPI-tree regen warnings), aligning its name with the Capability/Feature/Story/PI Canvas family. "Export Diagnosis" renamed to "Export"; the diagnostic DOCX report's title now reads "{Product Name} - Experiment Canvas," matching Outcome Pulse's own export-title convention.

**Fixed — a stale "Sent" indicator could survive deleting an experiment's Feature Canvas card:** the sent-state cache was only ever rebuilt on session load or a live-sync event, never on card deletion. Now rebuilt immediately after any Feature Canvas feature deletion.

## v9.10.03 — Outcome Verification Loop third feedback round

**Fixed — typing a single character into Feature Name silently triggered a real AI generation call:** the previous round's fix (auto-switching the modal's highlight from "Enter manually" to "Generate with AI" the moment a name is entered) called the same function used for a real button click — which, as a side effect, always also fires generation. Split into a repaint-only function used by the silent auto-switch, and the original function (repaint + trigger) reserved for actual user clicks.

**Fixed — the on-demand hypothesis-generation call was never registered with its own AI model default,** silently falling through to a generic fallback model regardless of what a deliberately-chosen default should be — meaning it also ignored the "Optimized" model setting's intent to route different call types to different tiers. Registered explicitly at the Haiku tier, matching the profile of other single-item (not bulk) generation calls already in the same table.

**Changed — secondary hypothesis Direction is now a real, clickable dropdown, matching the primary hypothesis exactly:** the previous round had made it a muted, read-only "Auto ↓/↑" label to fix a different complaint (it visually resembled a control it wasn't) — but this created a new, worse inconsistency: two Direction fields in the same modal, styled and behaving completely differently for no reason a user could perceive. Reversed per explicit instruction to keep primary and secondary uniform. Secondary's Direction now has its own manual-override tracking, mirroring primary's discipline exactly — editing baseline or target never silently discards a direction a person deliberately picked.

**Fixed — the Outcome Breakdown "Baseline to Target" column showed a bare dash whenever only one of the two values existed,** silently discarding the one number that did exist. The underlying check required both values to be present before showing either — now each side is checked and formatted independently, so a feature with only a target recorded shows "— to 5", not just "-".

**Fixed — the PDF export title change from the previous round was never actually built,** despite being on the list — it still showed a plain, small product name with no "Outcome Pulse" suffix. Now combines both into one line at a font size matching the North Star Metric's own hero number, using a plain hyphen rather than an em dash.

**Added — AI-generated outcome hypotheses now explicitly disclose that their baseline and target are industry-plausible estimates, not the client's actual current data.** Investigated and confirmed: neither hypothesis-generation prompt (the primary Capability Canvas path or the on-demand Add Feature path) ever supplies real client data to ground these numbers — the AI produces a statistically plausible guess for a metric of that type, with no mechanism to signal when its confidence in that guess is low. Rather than changing the underlying generation logic (a larger, separate discussion), both prompts now require the AI's own rationale sentence to end with an explicit, plain-language note to this effect.

**Testing note:** added 21 new targeted tests this round (161 total across all rounds, all passing), including a full re-verification that the real "Generate with AI" button click path still correctly triggers generation after the auto-switch fix, and a caught-and-fixed false positive in the test suite itself — an early version of one assertion string-matched a code comment that merely *mentioned* a function name (explaining why it wasn't being called) as if it were an actual call, and had to be corrected to strip comments before checking.

## v9.10.02 — Outcome Verification Loop second feedback round + Last Updated column

**Fixed — the feature card hypothesis chip's tooltip never appeared on hover:** the chip's `overflow:hidden` (needed for text-ellipsis truncation) also clipped its own CSS-generated tooltip pseudo-element, since both behaviors lived on the same element — a real, unavoidable conflict, not a z-index or click-propagation issue. Fixed by splitting the chip into an outer tooltip-anchor (no overflow) and an inner truncating span, matching the identical pattern already proven elsewhere in this codebase (`home.js`'s session-document filename tooltip). Applied to both places this chip appears — Feature Canvas's card footer and Capability Canvas's right-panel feature rows — after finding the second instance had the identical structure, per adversarial review.

**Fixed — a feature's existing secondary hypotheses disappeared when reopening Edit Feature, reappearing only after clicking "+ Add":** the modal-opening function correctly seeded its working array from the feature's saved data, but the container that's supposed to display those rows was only ever painted by a function called from later user actions (add/remove/update row) — never automatically once the modal's own HTML was added to the page. Fixed by calling that render function once, explicitly, right after the modal is added to the page.

**Fixed as part of the same investigation, not a separate report:** found via adversarial review that neither Add nor Edit Feature modal guarded against being opened twice in quick succession (e.g. a fast double-click), which — combined with both modals using a fixed, non-unique element id — could have caused the secondary-hypothesis fix above to paint into a stale, previously-opened instance of the modal instead of the current one. Added a guard to both modals that removes any existing instance before opening a fresh one, closing this off structurally rather than leaving it as a currently-unlikely-but-real edge case. Also added a matching safeguard to the on-demand AI hypothesis generation feature, which involves a real network wait — if the modal that started that request is closed (or replaced by a fresh instance) before the response arrives, the result is now discarded instead of being written into whatever happens to occupy the same element ids at that point.

**Changed — the secondary hypothesis row's Direction indicator no longer looks like a clickable dropdown:** it was never actually one (direction is computed automatically for secondary items, by design), but it borrowed the same purple pill styling as the primary row's real, clickable Direction control, leading a tester to reasonably expect the same behavior. Changed to a muted grey label reading "Auto ↓" / "Auto ↑" / "Auto —", with a plain cursor and no border, so it now reads as informational rather than interactive.

**Added — a "Last Updated" column to Outcome Breakdown**, between Signal and Action, showing a plain relative-time string (e.g. "5d ago") for when a result was last logged against that feature's primary hypothesis — "—" if never logged. Reuses the exact same plain-text, no-color-warning pattern already established for the North Star Metric card's own "Updated" field, per explicit design decision that this app never shows a staleness warning treatment anywhere. Column widths across the whole Outcome Breakdown table were rebalanced to make room for the 7th column.

**Changed — features within each Outcome Breakdown stage are now sorted alphabetically by name**, previously in unspecified insertion order.

**Changed — signal colors and casing, for consistency:** "No Change" now displays in amber (previously a neutral grey indistinguishable from "Not Applicable"); "Not Applicable" now uses a distinct grey. All signal values ("Aligned", "Opposed", "No Change", "Awaiting", "Not Applicable") and the ledger's "Log Result" action now use consistent Title Case throughout — the distribution bar, count grid, legend, filter dropdown, ledger badges, and the unified hypothesis modal's signal-selection pills all updated together. The signal-selection pills in that modal also now highlight in their own semantic color when selected (green for Aligned, red for Opposed, amber for No Change, grey for Not Applicable) rather than every option turning the same green regardless of which was chosen — found and fixed in two places (the initial render and the click-handler repaint), since both had independently hardcoded the same color.

**Changed — the Outcome Breakdown filter** relabeled from "All statuses" to "All Signals" for consistency with the "Signal" terminology already used everywhere else in this feature (the ledger's own column header, the legend, the modal), and restyled to match the exact filter control already used on the Home page (filter icon + dropdown), rather than a plain unstyled `<select>`.

**Changed — "Download report" renamed... already renamed to "Export" in the previous release; this release adds:** the product name now displays significantly larger in the exported PDF's header (matching the North Star Metric's own hero-number visual weight), combined with the report title into a single "Product Name – Outcome Pulse" line using a plain hyphen rather than an em dash.

**Changed — Discovery Brief DOCX export:** the Hypothesis column's "Primary:"/"Secondary:" label is now bold on its own, with the metric name that follows in normal weight — previously the entire line (label and metric name together) was bolded as one unit. Required extending the shared table-building helper to support mixing bold and normal text within a single line, not just multiple stacked lines.

**Changed — Capability Canvas right-panel feature rows:** the "In Feature Canvas" tag and the hypothesis chip now sit side by side on the same line instead of stacked, with matching size/padding so both read as the same visual weight — previously the tag was visibly smaller than the chip next to it.

**Changed — Add Feature modal:** the "Enter manually" / "Generate with AI" highlight now automatically switches to "Generate with AI" the first time a feature name is entered, guiding the user toward the faster path. Only fires once per modal-open — a user who deliberately clicks back to "Enter manually" afterward won't have that choice silently reverted on their next keystroke.

**Testing note:** this round added 38 new targeted tests on top of the previous round's 87 (125 total, all passing), including a simulated-DOM test that functionally proves the secondary-hypothesis render fix writes into the correct modal instance and never a different one. One additional real finding during self-review (not from the external adversarial pass): the signal-pill color fix needed to be applied in two separate places — the modal's initial render and its runtime click-handler repaint — both of which had independently hardcoded the same incorrect color.

## v9.10.01 — Outcome Verification Loop feedback fixes (first round of live testing)

**Fixed — secondary hypothesis fields overflowed the Edit/Add Feature modal's right edge:** the Outcome Hypothesis section's 4 grid rows used raw `1fr` tracks, which default to `min-width:auto` in CSS Grid — letting fixed-width columns (Unit, Direction, remove-icon) push the row past the modal's 400px boundary since nothing constrained horizontal overflow (only vertical scroll existed). Fixed with `minmax(0,1fr)` on every flexible track plus `min-width:0` on the grid cells themselves.

**Changed — Secondary section label** to "SECONDARY HYPOTHESIS (max 2)", dropping the redundant "(optional".

**Changed — feature card hypothesis chip:** hovering a populated chip now shows the full metric name plus baseline → target (previously just repeated the same truncated text already visible on the chip, adding no information). An unset hypothesis no longer shows a passive "No hypothesis" label — it's now a clickable "+ Define hypothesis" chip (purple, dashed border, hover-fill) that opens Edit Feature directly; read-only users still see the original plain, non-interactive label.

**Added — the same hypothesis chip to Capability Canvas's right-panel feature list**, distinct in color from the existing stage-colored breadcrumb pill already shown there (confirmed via code read: that pill's color comes from `ccStageBg()`/`ccStageText()`, a fixed 4-stage lookup — the new chip deliberately does not reuse that scheme, reusing Feature Canvas's purple hypothesis-chip styling instead so the two represent visually distinct kinds of information). Built as its own function rather than reusing Feature Canvas's chip builder, since a capability-drawer feature may not yet exist on `scCanvas` — reusing that builder's click handler would have opened Edit Feature for a feature that couldn't be found.

**Changed — Discovery Brief DOCX export:** the per-feature table's "Origin" column is replaced with "Hypothesis" (Primary + Secondary, each showing metric and baseline → target, "Not set" when absent). Required extending the shared `_docxTable3Col()` table builder to support multi-paragraph cells — previously hardcoded to exactly one line of text per cell.

**Added — hypothesis line in the Story Canvas / feature-detail DOCX export**, inserted directly after the existing italic "why it matters" line: one paragraph, bold "HYPOTHESIS" label, natural Word-wrap, omitted entirely for features with no hypothesis.

**Fixed — Outcome Breakdown table columns didn't align between different value-chain stages:** each stage rendered its own independently auto-sized `<table>`, so a stage with longer metric names ended up with different column widths than its neighbors. Fixed with one shared `<colgroup>` (percentage-based, `table-layout:fixed`) applied to every stage's table, plus `overflow-wrap:anywhere` so long unbroken text wraps instead of forcing a column wider.

**Changed — "N hypotheses · M features" reordered to "N features · M hypotheses"** consistently across the Hypothesis Health summary card and every stage row, regardless of stage count.

**Changed — "Download report" renamed to "Export"**, now using the exact same button class and icon (`.export-cta-btn`, `ti-download`) already used by the Feature Canvas, Capability Canvas, Story Canvas, and PI Canvas export actions, rather than a one-off custom-styled button.

**Fixed — the North Star Metric edit overlay visually extended past the card's own boundary**, overlapping content beneath it. Replaced the `position:absolute` overlay (which pinned position but never constrained height) with a real modal — the same reusable modal pattern used everywhere else in the app, which cannot exhibit this class of overflow by construction. The new modal also adds: an editable metric name (writes to `gData.nsm.metric`, the same field Discovery Map's NSM display already reads — no separate write-back step needed), a unit field (fixed list + custom-label escape hatch, matching the existing hypothesis-unit pattern), and splits into a hero "current actual" field plus a collapsed "Edit metric definition" section that auto-expands only when baseline and target are both strictly unset (a real baseline/target of `0` is never mistaken for "not set," per an explicit null check rather than a falsy check). A cross-tab consequence notice appears only once the definition section is expanded.

**Added — thousands-separator formatting** (e.g. "7,000" instead of "7000") applied everywhere a metric value is displayed across Outcome Pulse — the North Star Metric hero number and footer, the Outcome Breakdown ledger's baseline/target/actual columns, and the unified hypothesis modal's read-only hypothesis display. Live input fields are unaffected — only rendered/displayed values are formatted.

**Fixed — the North Star Metric and Hypothesis Health cards showed visibly different heights.** The parent row's `align-items:flex-start` was deliberately chosen earlier specifically to prevent the (now-removed) overflowing NSM overlay from stretching and distorting its sibling — with that overlay gone, switched to `align-items:stretch` with `height:100%` on both cards' root elements.

**Fixed — the PDF export ("Preparing…" loading state leaking into the exported PDF, no page margins, no product name):** the export button lived inside the same DOM subtree being captured by `html2canvas`, so its loading-state mutation was always guaranteed to appear in the screenshot regardless of timing. Restructured so the button is a sibling outside the captured `#op-export-target` element entirely, with a dedicated export-only header (hidden during normal use, populated with the centered product name only for the instant of capture) rendered inside the captured region instead. Added real 36pt margins on all 4 sides of the PDF placement math, and fixed a pagination bug found while rewriting this (a duplicate, conflicting Y-offset calculation across page breaks) with a single clean per-page formula.

**Fixed — Outcome Pulse tab visibility was never correctly recalculated during session resume:** confirmed via code read that `applyFeats()` — where the tab's reveal condition lives — is never called by `sessionStoreRestore()` at all; a separate, independent function (`_ssSyncTabVisibility()`/`_ssRevealTabs()`) handles tab visibility specifically during resume, and it had no knowledge of the Outcome Pulse tab whatsoever. Added the same combined condition (feature flag on AND the resumed session's snapshot has `gData`) to that function directly, plus added `tab-op` to its hide-phase so a resumed session without a Discovery Map correctly hides the tab rather than leaving it in whatever state it happened to be in before resume started. Also corrected `applyFeats()`'s own reveal condition, which previously revealed purely on the feature flag with no `gData` check at all — the tab now requires both flag and a generated Discovery Map to become visible on initial load or a live Settings change, matching PI Planning's existing "only reveal once real content exists" pattern rather than Market Intelligence's "reveal purely on flag" pattern.

**Testing note:** this round was verified with 87 automated tests (62 unit tests on the pure computation helpers covering null/zero/negative/overshoot edge cases, 13 permission-gating tests across read-only and editable states, 12 stage-grouping tests including a synthetic 7-stage non-AARRR framework) plus a real generated-and-rendered DOCX document visually inspected for every hypothesis-format edge case (no hypothesis, primary-only, primary + 2 secondaries, missing baseline/target, malformed empty metric). One real bug was found and fixed during testing: `computeDirection()` treated an empty-string input as a valid zero rather than "not set," though this was not reachable through any current UI code path since every caller already pre-normalizes empty strings before calling it. Browser-rendered visual confirmation of the CSS Grid fix and the html2canvas/PDF pipeline was not possible in this environment (no headless browser available) and should be confirmed in live testing.

## v9.10.00 — Outcome Verification Loop (new feature): hypothesis capture + Outcome Pulse tab

**Added — Outcome Hypothesis capture on features:** every feature can now carry a primary outcome hypothesis (target metric, unit, baseline, target, computed-but-overridable direction, AI-generated rationale) plus up to 2 lighter-weight secondary hypotheses. Generated automatically by AI alongside feature name/why-it-matters in the same Capability Canvas generation call (`buildCapFeaturesPrompt()`); a malformed or missing hypothesis in the AI response never blocks feature creation, it just leaves that feature hypothesis-less. Manually added features (`scDoAddFeat()`, drawer toggle) get the same capture UI in Add/Edit Feature modals, with full parity between the two — including an on-demand "Generate with AI" option (gated on Feature Name being filled) for manual-add. Feature cards show a hypothesis chip (metric + direction arrow) alongside the existing story-count label.

**Added — Outcome Pulse, a new tab** (feature-flagged, default OFF, toggle in Settings > Feature Modules): a leadership-facing dashboard closing the loop from hypothesis to logged result.
- **North Star Metric card** — hero number for the NSM's current actual, a plain-language trajectory read ("Tracking toward target" / "Flat" / "Moving away from target" — deliberately never causal language), baseline/target/actual editable via a hover-revealed pencil that opens an overlay pinned exactly to the card's own footprint (`position:absolute;inset:0`), so neither this card nor its neighbor ever resizes or reflows regardless of edit state. Baseline/target lock permanently after first save (only `actual` stays editable thereafter) — this is the first time this app has ever tracked a numeric NSM value; previously `gData.nsm` held only `metric`/`definition`.
- **Hypothesis Health card** — aggregate signal distribution (Aligned/Opposed/No change/Awaiting/Not applicable) with an always-visible inline legend, computed via one shared `computeHypothesisAggregates()` function that every surface in this feature consumes — deliberately never recomputed independently per surface, to guarantee the numbers agree everywhere (dashboard, breakdown, PDF export).
- **Outcome Breakdown** — one row per Discovery Map value-chain stage, **derived from the live `gData.stages` array at render time** (never hard-coded to a fixed 4-stage list, since stage sets vary by industry/framework routing and stages are user-renameable), with an "Unmapped" bucket for any feature whose stage doesn't currently match. Expanding a stage row shows full per-hypothesis detail inline (metric, baseline→target, actual, signal badge, single "Log result" action) — no separate ledger view elsewhere. One filter: signal status only (no separate stage filter, since stage is already the row grouping).
- **Unified hypothesis modal** — the single "Log result" action on every row opens the same modal regardless of state (pre-filled if a result already exists). Shows the full read-only hypothesis plus an editable actual-value field and four signal pills (Aligned/Opposed/No change/Not applicable) — the PM can select any signal at any time, with or without a numeric actual entered; a numeric actual only ever produces an advisory "Suggests: X" hint via `computeSuggestedSignal()`, never a requirement or an auto-applied value.
- **PDF export** ("Download report") — new export mechanism for this app (every prior export produces a structured DOCX/XLSX from data, never a rendered-screen capture); reuses the same lazy html2canvas CDN-load pattern already established in `prototype-canvas.js`, paired with jsPDF, paginating by slicing the captured canvas into page-height chunks since Outcome Breakdown can expand to arbitrary height.

**Scope, by explicit decision, not oversight:**
- Bulk-uploaded features (`scConfirmFeatUpload()`) do not get hypothesis capture in this release — deferred to a future version. Every other feature-creation path (AI generation, manual single-add, drawer toggle) does.
- No Settings-configurable unit-of-measure list — a fixed default list (%, days, hours, minutes, seconds, currency, count, score/rating) plus a "custom label" escape hatch, matching the same complexity-control decision already made for other configurable lists in this app.
- NSM manual edits reuse the **existing** `mm`-canvas wholesale-apply/confirm-gate live-sync mechanism rather than a new dedicated event type — simpler, proven, at the cost of coarser conflict granularity than a per-field sync would give.
- No reverse-feed from a missed hypothesis into auto-generating new stories/features — a real idea, explicitly out of scope for this release.
- "Shipped" is not a concept this feature introduces or depends on — no build/release lifecycle state exists anywhere in this app's data model (confirmed via code read before deciding this, not assumed). Any feature carrying an `outcomeHypothesis` is "tracked," full stop; dashboard copy says "N hypotheses tracked across M features," never "shipped features."

**Fixed as part of this build, not a separate item:** `gData=parsed` in the Discovery Map generation/refinement flow (`kpi-tree.js`) is a full reassignment on every single call — without an explicit preservation step, any previously-entered NSM baseline/target/actual would have been silently wiped on every regeneration or refinement. A snapshot-and-restore step was added around this reassignment, mirroring the existing pattern already used for `kpiDepth`/`marketIntelligenceEnabled` just below it.

## v9.09.01 — Fix Read Only file-upload bypass; role-menu label casing

**Fixed — Read Only users could still upload and process a capability list or session document on the Home tab's left panel:** the v9.09 fix targeted `#home-sdocs-box` with a one-time `querySelector('button')` disable, but that element is a `div`+`onclick` (not a `<button>`), and the box gets fully rebuilt by `homeRenderSdocsSection()` at ~7 call sites throughout the file, silently discarding the disable on every re-render. Fixed at the actual source — the render function itself now checks role before wiring the upload trigger, and both upload handlers gained an independent guard as defense-in-depth. Also fixed in the same pass: Team Management's row-action menu showed lowercase role names ("Make admin") inconsistent with how roles are capitalized everywhere else in the app (badges, invite tiles) — now reads "Make Admin" / "Make Power User" / "Make Read Only".

## v9.09 — Power User rename; new Read Only role

**Added — a third company role, Read Only, alongside the renamed Power User (formerly "Regular User", internal value unchanged):** Read Only users get identical Settings access to Power User, cannot launch new sessions under any circumstance (Home tab's setup form stays visible but every control is disabled, with an inline notice), and can view sessions shared with them exactly as any other viewer of a "View only" shared session — reusing the existing v9.08 enforcement unmodified. Team Management's invite modal, role filter, and row-action menu (now showing only the valid "make X" transitions per row, not a binary toggle) all support the third role.

**Fixed — closed a role-authorization gap found in adversarial review, not present in the original v9.08 view-only build:** `canEditSession()` previously checked session ownership before role, meaning a user demoted to Read Only who still owned older sessions retained full edit rights on them. Role is now checked first and fails closed on any unrecognized or unloaded role — this also means Read Only now correctly overrides even a session explicitly shared in "Collaborative editing" mode, a deliberate decision (role wins over share mode).

**Fixed — two SECURITY DEFINER database functions (`save_shared_session_content`, `acquire_generation_lock`) were role-blind:** RLS policies alone don't protect these, since SECURITY DEFINER functions bypass RLS by design — both now independently check for an active, non-readonly company membership before writing. Three `mt_sessions` RLS policies (INSERT/UPDATE/DELETE) also gained the same check, closing a related gap where a demoted user retained direct database write access to sessions they own.

**Fixed — `/api/team/set-role` now hard-fails on an unrecognized role value instead of silently defaulting it to Power User**, closing a privilege-upgrade path a malformed or malicious request could previously exploit. `/api/team/invite` keeps its existing default-to-Power-User behavior for an omitted role, which is unaffected.

## v9.08.06 — Fix Process Area's ungated capability edit/delete icons

**Fixed — Process Area-approach sessions' Discovery Map cards still showed clickable edit/delete icons for view-only sessions.** Outcome Metrics' equivalent L1 row was correctly gated back in the original view-only build, but Process Area capabilities render through a completely separate template branch (`data.approach==='capability-based'` vs. the Outcome Metrics `else`), which was missed since the two branches don't share scope. Both approaches now correctly hide these icons for view-only sessions.

## v9.08.05 — Fix second, previously-missed Evidence CTA label path; fix phantom Dictionary generated-state check

**Fixed — the Discovery Map card's "Evidence"/"Edit evidence" button still showed "Edit evidence" for view-only sessions, even after the previous fix.** The previous round's fix targeted `dvEvidenceTriggerLabel()`, the function used at initial render — but a second, entirely separate function (`kpiRenderEvidenceStates()`) runs afterward as a DOM-sync pass (triggered on session restore and after evidence saves/diagnostic runs) and unconditionally overwrote the button's label based only on whether evidence existed, bypassing edit-permission entirely. Both copies now correctly check edit permission before showing "Edit."

**Fixed — the Dictionary panel's generated-state indicator showed an empty box instead of either the generate button or the "generated" confirmation.** The underlying check tested for `document.getElementById('dd-out')`, a DOM element that is never actually created anywhere in the app — a dead/phantom check that happened to go unnoticed before dictionary data was shared between collaborators, since it rarely mattered when nothing was shared in the first place. Replaced with a real check against the actual persisted dictionary content, and removed an identical phantom check used in the per-metric Definition/Benchmark/Red Flag display logic.

## v9.08.04 — Fix share_mode dropped on Home poll; persist Dictionary data across collaborators

**Fixed — a shared session's access mode (View only vs Collaborative editing) was silently lost by the Home screen's background poll cycle:** `_lsMergeHomeMetaEntry()`, which runs on every recurring poll of shared sessions, rebuilt each session's local metadata without including `share_mode` at all — and neither did the underlying database query it reads from. This meant a correctly-saved "View only" session's client-side state could be overwritten with nothing, recurringly, in the background, independent of anything the user did — most visibly seen as a non-owner collaborator being shown edit-only affordances (like "Edit evidence" instead of "Evidence") on a session that was genuinely view-only. The same missing field was also found and fixed in the one-time resume pre-fetch path. Both now correctly carry the session's real access mode.

**Fixed — Discovery Map's Dictionary data was never actually shared between collaborators.** The "dictionary generated" flag was being saved with the session, but the underlying generated content (definitions, benchmarks, red flags per metric) was not — so any collaborator opening the session in their own browser saw an empty dictionary regardless of what a teammate had already generated, and reopening a metric's dictionary would silently trigger a full, redundant regeneration. The actual data now saves and restores alongside the flag, matching the same pattern already used for Market Intelligence's data.

## v9.08.03 — Continued view-only completion; export regression fix; Output Depth wireframe

**Fixed — a genuine regression: Feature Canvas's Export button was incorrectly disabled for view-only sessions.** The previous pass's action-bar visibility fix used an early return that accidentally also skipped Export's own enable/disable logic, even though Export lives in a separate toolbar wrapper outside the action bar's actual scope and must always reflect "is there anything to export," never edit permission. Restructured so Export's logic runs unconditionally, before any edit-mode branching.

**Added — 8 further mutation entry points found only through continued testing, none previously gated:** Discovery Map's Dictionary panel "Generate for All Metrics" button, and its Evidence trigger label (now reads "Evidence" instead of "Edit evidence" when view-only, since the label was misleading about what clicking it does); Capability Canvas's per-metric empty-state "Generate Capabilities"/"Generate for All" buttons; Product Diagnostics' detail-panel "Add to Selection" button (a second render site for an action already gated at the table-row level); and five further gaps inside Feature Canvas's story panel — the "All"/"None" PI-selection buttons, individual per-story PI-selection checkboxes, all three inline story-attribute edit pencils (title/description/acceptance-criteria) plus their save handlers, and the panel-level "Generate Stories" button for a feature with no stories yet.

**Fixed — Output Depth settings page:** tightened the visual gap between two row-pairs meant to read as one group (Capabilities per Metric/Include Sub-Capabilities; Stories per Feature/Acceptance Criteria per Story), matching the actual measured spacing already used elsewhere on the page rather than guessing a new value. Replaced Session Sharing's bordered-box container with a plain grey top divider, consistent with how every other section on this page separates from the one above it.

**Documented, not fixed — `ccDDDownload()`'s fallback path:** this function can trigger dictionary generation as a side effect if called before generation has happened, and is deliberately left unguarded by permission checks (to guarantee export always works). Its one current call site is safely preconditioned; flagged in `PROJECT_MAP.md` so a future new caller doesn't assume it's unconditionally safe.

## v9.08.02 — View-only enforcement completion across all 7 canvases

**Fixed — View-only sessions still showed fully-interactive controls that silently no-opped, rather than being visually locked:** The original v9.08 build correctly gated every mutation at the handler level (confirmed: nothing persisted for view-only collaborators), but was inconsistent about the visual layer — many cards, checkboxes, toggles, and buttons across Discovery Map, Capability Canvas, Feature Canvas, Story Canvas, PI Planning, and Prototype Canvas still looked fully clickable, producing a confusing "why didn't that work" experience. All affected surfaces now follow one consistent rule: standalone action buttons (Add, Generate, Send to X, Refine, Remove-as-button) are hidden; toggles, checkboxes, inline-editable fields, and dropdowns are disabled/readonly. This is now a standing project rule for all future permission-gated work, documented in `AI_EDITING_RULES.md`.

**Fixed — Story Canvas's Definition of Ready toggle visually changed even though it never persisted:** The underlying bug was a `readonly` attribute on a `<input type="checkbox">` — `readonly` has no effect on checkboxes in any browser, only `disabled` does. The toggle now uses `disabled` and correctly shows as locked, matching its actual behavior.

**Added — Several mutation entry points that were never gated at all in the original build:** Discovery Map's tree-level "Refine Discovery Map"/regenerate flow and value-stage edit/delete; Capability Canvas's inline feature name/description editing and the feature-selection checkbox (including the specific bug where clicking an already-sent feature's checkbox could trigger and complete a removal, later found not to persist); Feature Canvas's edit-feature modal and both card-checkbox variants; Story Canvas's panel-level "Remove story from canvas" flow (distinct from the card-level delete already gated); PI Planning's "Remove from PI?" confirmation and the separate backlog-panel points/sprint/notes fields; and the entirety of Prototype Canvas (generate/regenerate across all 5 render states, screenshot upload/remove, and refine context) — a full screen that was absent from the original 29-entry inventory.

## v9.08.01 — Share/unshare crash fix, Output Depth polish

**Fixed — Sharing or unsharing any session threw `ReferenceError: entry is not defined` and silently failed to persist to the database:** `homeSessionToggleShare()`'s async DB-write block referenced a variable (`entry`) that was scoped only to the synchronous block above it, out of scope by the time the database call ran — on every single toggle, both share and unshare. The session card and toast looked correct locally (localStorage updated fine), but the underlying Supabase row never actually changed, meaning collaborators wouldn't see the new share state at all. Fixed by hoisting the needed value into function scope before the async block runs. *(This patch also folds in two small Output Depth settings-page polish items reported in the same testing pass — divider spacing between grouped rows, and the Collaborative Editing warning strip now showing/hiding correctly based on the actual selected mode rather than always rendering. Per versioning convention a patch is normally one fix; these three were found and fixed together in one round and are recorded together here for traceability rather than split into three near-simultaneous patch numbers.)*

## v9.08 — Read-only shareable sessions

**Added — Session sharing now has two modes: View only (new default) and Collaborative editing:** Previously, sharing a session always granted collaborators full read/write access, identical to the owner's own permissions. A new company-level setting (Settings → Output Depth → Session Sharing) controls the default for newly-shared sessions, defaulting to View only. View-only collaborators can view and export session content but cannot generate, edit, or delete anything — across all 7 canvases (Discovery Map, Capability Canvas, Feature Canvas, Story Canvas, PI Planning, Product Diagnostics, Market Intelligence). This is intended to reduce accidental concurrent-write conflicts, which the app's existing full-snapshot save model (no version checking) is vulnerable to.

**Enforcement is server-side, not cosmetic:** The two RPCs that allow a non-owner to write to a shared session (`save_shared_session_content` for content saves, `acquire_generation_lock` for AI generation) now check the session's `share_mode` directly in their authorization logic — a view-only collaborator's client cannot bypass this by calling the RPC directly, since the database itself rejects the write. Client-side UI hiding (the ~29 gated entry points across all screens) is a UX layer on top of this, not the actual security boundary.

**Freeze-on-open for viewers:** A view-only collaborator's client never starts the live-sync watch/poll cycle for that session — they see the snapshot as of when they opened it, with the next fresh fetch happening on their next resume. The owner can continue editing in the background; the viewer simply doesn't see live updates mid-session, consistent with the existing resume-refetch behavior already used for shared sessions.

**Adversarial review process:** This feature went through two rounds of external adversarial review before being built. Round 1 found and closed: a revocation race in the original two-statement RPC authorization pattern (fixed by collapsing into a single conditional `UPDATE ... RETURNING`), a completely separate, previously unaudited write path (`acquire_generation_lock`, which had no `share_mode` awareness at all), and a fail-open client-side default (`_activeSessionShareMode` now defaults to `'view'`, not `'edit'`). Round 2, scoped narrowly to the lock-RPC fix, found and closed a ghost-holder display bug (`release_generation_lock` wasn't clearing `active_user_name`) and tightened `search_path` hardening on both lock RPCs to match the save RPC's existing pattern.

**Known non-issues, checked and confirmed clean:** All 5 export functions (PI, Market Intelligence, Diagnostic, XLSX, general DOCX) were checked line-by-line for hidden state-mutation side effects — none found, all remain fully available to view-only users. Two functions referenced in earlier planning (`piSyncNewStories`, `piHandlePrevPIFile`) turned out to be dead code / already-decommissioned features respectively, confirmed via full-codebase grep before deciding no fix was needed for either.

## v9.07 — Outcome Metrics edit/delete parity, downstream-detection fixes, Discovery Map live-sync

**Added — Outcome Metrics L1 rows now support edit and delete, matching Process Area mode:** Previously only Process Area's flat capability rows had pencil/delete controls; Outcome Metrics' L1 metric rows only had a decorative expand chevron. Both modes now share the same edit/delete path, including full cascade delete of nested L2/L3 children. The expand chevron itself is now only shown (and only clickable) when a metric actually has nested children, instead of always appearing.

**Fixed — Deleting a metric or stage with generated-but-unsent Capability Canvas data was a silent, unwarned data loss:** The downstream-impact check only looked for capabilities whose features had been pushed to Feature Canvas — capabilities sitting in Capability Canvas that were never sent onward weren't counted, so deleting their parent metric or stage removed them with no warning at all. Also fixed a related, more severe pre-existing bug found while investigating this: deleting a whole stage never actually cleaned up its Capability Canvas data in the first place (a stale key-matching bug), silently leaving orphaned data behind on every stage deletion until now. Both cases now show an explicit warning naming the exact capability count before deletion, and stage deletion now genuinely removes what it claims to.

**Fixed — Rapid double-clicks and background changes during edit/delete could corrupt the wrong item:** Confirm buttons are now disabled the instant a delete is submitted, preventing a double-click from deleting twice. Edit and delete also now re-verify the item hasn't changed identity since the modal was opened (e.g. from a teammate's simultaneous edit), aborting safely with a message to retry rather than silently mutating a different item than the one shown.

**Added — Discovery Map stage and metric edits/deletes now save and sync for shared sessions:** These actions previously only updated the local screen and were never persisted or broadcast to collaborators — a page refresh, or a teammate's client, would silently show the pre-edit state. They now save immediately and notify collaborators, consistent with every other part of the app. This is a lightweight, non-blocking save (a courtesy warning appears if someone else is actively generating on the same session, but the save is not blocked or queued) rather than a fully serialized write — a known, accepted simplification for now.

## v9.06.02 — Custom process areas now render like AI-generated ones, with correct rename propagation

**Fixed — Custom process area layout inconsistent with AI-generated ones:** Custom Value Stage previously used a special two-level grouping (one outer banner wrapping multiple process areas) unlike every AI-generated stage, where each process area gets its own header. This caused missing banners and misordered cards once a second custom process area was added. Custom process areas now render identically to AI-generated ones — including in Capability Canvas's sidebar, where clicking a specific custom process area now shows only that one's capabilities, matching existing behavior for AI-generated process areas.

**Fixed — Renaming Custom Value Stage or a custom process area didn't visibly update:** The stage name display was being unconditionally overridden regardless of what the user typed. Renaming a specific process area now also updates any features already placed on Feature Canvas and Story Canvas, so their navigation panels and traceability details show the new name instead of the old one.

**Fixed — Feature Canvas and Story Canvas grouped all custom process areas together:** Previously merged under one generic label regardless of which specific process area a feature belonged to. Now grouped by the actual process area name, consistent with how AI-generated metrics already group.

**Fixed — Extra explanatory text in the process area edit dialog:** Removed a note specific to custom process areas that didn't appear for AI-generated ones, so the dialog now looks the same for both.

## v9.06.01 — Custom Value Stage rename support and dropdown improvements

**Added — Custom Value Stage name is now editable, defaulting to "Custom Value Stage":** Previously locked to prevent breaking internal matching; the underlying matching logic is now identity-based instead of label-based, so renaming is safe. Existing sessions still showing the old internal placeholder name are migrated automatically on load; sessions already renamed by a user are left untouched.

**Added — Add Capability dropdown lists individual custom process areas/metrics:** Previously only one generic option was available. The generic option remains first and unchanged; each existing custom process area/metric now also appears as its own selectable option, matching how AI-generated metrics already work. Applies to single capability add, bulk upload, and reassigning an existing capability.

**Fixed — Discovery Map lost horizontal scroll position after editing a stage:** Particularly noticeable with Custom Value Stage sitting far to the right. Scroll position is now preserved across the re-render.

**Fixed — Traceability panel (Feature Canvas and Story Canvas) now shows the stage name** for capabilities under Custom Value Stage, instead of hiding it; visual spacing between multiple custom process areas in Capability Canvas's main panel also corrected.

## v9.06 — Custom Value Stage display and sync fixes

**Fixed — Discovery Map showed "PI Plan" instead of "Custom Value Stage":** The internal stage label was rendered raw in Discovery Map's stage header. Now relabeled for display, matching the convention Feature Canvas and Story Canvas already used.

**Fixed — Capability Canvas left panel showed a stale, combined count immediately after adding a capability:** The sidebar summarized all custom process areas/metrics as one row regardless of how many distinct ones existed, causing a mismatch with the main content area (which correctly separates them) until an unrelated re-render papered over it. Sidebar now shows one row per distinct process area/metric, matching the main content area.

**Fixed — Redundant sub-header when only one custom process area exists:** The bucket-level sub-heading inside "Custom Value Stage" is now only shown when multiple distinct process areas/metrics exist; a single one no longer duplicates the group header above it.

**Fixed — Editing or deleting a custom process area in Discovery Map didn't refresh Capability Canvas:** Renaming or deleting now also refreshes Capability Canvas's left panel and main view when it's the active tab, in addition to Feature Canvas and Story Canvas (which already refreshed correctly).

## v9.05 — Custom Value Stage now visible in Discovery Map

**Added — Custom capabilities now appear in Discovery Map:** Capabilities tagged to "Custom Process Area"/"Custom Metric" in Capability Canvas now show up as a "Custom Value Stage" in Discovery Map. Multiple independent process areas are supported — renaming one frees up the default name for a fresh one, matching how Capability Canvas already groups them. Existing sessions with capabilities added before this release are migrated automatically on load.

**Changed — Custom Value Stage editing rules:** The stage's own name can no longer be changed (prevents breaking Feature/Story Canvas's internal matching logic), though its description remains editable. Deleting or resetting the whole stage now correctly removes every custom process area and capability beneath it, with copy that says so explicitly. Individual process areas within the stage remain fully renameable and deletable.

**Fixed — Live-sync now reflects custom process areas for shared-session collaborators:** No new sync event was needed — Discovery Map re-derives its Custom Value Stage from Capability Canvas's existing sync mechanism, so teammates see new/renamed/deleted process areas without a separate notification.

**Known limitation:** Two users creating a new custom process area at the same moment in a shared session may each end up with an independent bucket rather than merging — consistent with the concurrent-edit limitation already documented in v9.03.

## v9.04 — Story Canvas badge sync after backlog removal

**Fixed — Story Canvas badge stale after PI backlog removal:** When a user removed a story from the PI backlog tray, the story disappeared from the tray but in Story Canvas it still showed "In PI plan" badge. Root cause: `piFindStory()` returns shallow copy, so mutation of `_inPIPlan=false` wasn't persisted to original story object. Now finds and mutates original story in scCanvas directly, then calls `newScRender()` after save succeeds to refresh SC display.

**Works for both private and shared sessions:** Refresh happens regardless of session type (user requested both). No functional change for shared-session live-sync (receiver also sees updated badge after event arrives).

**No cross-render race condition:** `newScRender()` called only after save completes and persists. If user switches tabs mid-save, SC refresh still happens correctly when promise resolves. If story not in scCanvas, fallback checks piStoryPool.

**Known: One-way sync only:** This fixes PI→SC sync. Reverse sync (SC→PI) not in this fix — documented for separate work if needed.

## v9.03 — Backlog story removal, prev-pi cleanup, live-sync rule hardening

**Added — Remove story from PI backlog tray:** backlog cards now display a "Move to Story Canvas" button (✕, hover-only) that removes the story and returns it to Story Canvas's "Not Selected" state. Includes state snapshot, save-failure revert, and live-sync event emission for shared sessions.

**Removed — Previous PI / backlog input option:** Orphaned "Previous PI / backlog" radio button and all supporting code (hints, maps, parsing) from Capability Canvas. Old sessions with `type='prev-pi'` migrate at load time to `type='caps-only'` without data loss.

**Hardened — Live-sync event emission rule:** Rewritten in AI_EDITING_RULES.md to emphasize async value capture before `.then()`, state snapshot before mutation, explicit revert on save failure, and independent error handling for helpers.

**Known limitation — Concurrent shared-session edits:** Pre-existing architecture gap where stale local state can overwrite remote changes if two users edit simultaneously. Baseline `sessionStoreSave()` uses full snapshots without version checking. User workaround: refresh periodically. Phase 2 work to add version checking.

## v9.02.01 — PI Plan button/modal fix, diagnostic log cleanup, live-sync log suppression, title consistency

**Fixed — "Regenerate PI Plan" showing on first-ever visit to PI Canvas:** both the button label and `piGenerate()`'s confirmation-modal trigger checked bare `piPlan` existence rather than whether a real plan had been generated — since v9.01.01 correctly initializes `piPlan` minimally the moment stories are staged (to fix the tab-reveal/resume issue), this bare check now fired too early. Both now check for genuine generated content (`piPlan.sprints.length>0`), reusing the exact pattern already proven correct elsewhere in the same file. Confirmed via adversarial trace that both places that legitimately re-confirm a real regenerate still work correctly (their own `_isConfirmedRegen` short-circuit is unaffected).
**Removed:** the 3 temporary `[DIAG ...]` diagnostic console logs added in v9.01.01 to root-cause the PI-tab issue — root-causing is complete and the fix has been confirmed working, so these are no longer needed.
**Suppressed:** 7 verbose `console.log` calls in `live-sync.js` ("mark skipped" ×5, "flush skipped", "marked dirty") — confirmed each was a pure logging statement; every surrounding `if` condition and `return` is completely untouched, only the log output itself was removed.
**Title consistency:** Capability Canvas's main-content title now always reads "Capability Canvas" regardless of approach (previously flexed to "Process Areas" in Process-Area mode) — the left-panel header still reflects the chosen approach directly ("Process Areas" / "Outcome Metrics", with matching subtext) so that distinction isn't lost, just moved to a more appropriate spot. Market Intelligence's main-content title now always reads "Market Intelligence" (previously showed the product name plus a market/category-perspective label unrelated to this app's approach system) — both changes align every tab's main panel with its own tab name, matching the pattern other canvases already follow.

## v9.02 — Duplicate function definitions removed (code hygiene, no functional changes)

**Removed 6 duplicate top-level function declarations** discovered during v9.01.01's verification pass: `scPanelGenerateStories` (feature-canvas.js, two adjacent identical copies), `buildAIRecommendationsPrompt` (prompts.js, two non-adjacent identical copies with matching comments — confirmed ordinary copy-paste during extraction from home.js, not a botched move; home.js correctly only calls it, never redefines it), and one entire duplicated block in capability-canvas.js containing `ccRenderExportBtn`/`ccSyncExportBtn`/`_ccGetVisibleCapsSnapshot`/`ccExportDocx` plus a shared `var` initializer.
**Confirmed genuinely safe, not assumed:** all 6 pairs verified byte-for-byte or semantically identical via diff; JS's actual semantics mean the *second* declaration was live before this change (not the first) — correctly reasoned about explicitly, not glossed over. Every call site re-verified to resolve correctly post-removal. All 6 confirmed as true top-level declarations (no block/conditional/IIFE scoping edge cases). No alias/`window.x=fn`-style references found for any of the 6 — only direct calls. Each touched file confirmed loaded exactly once in `index.html`. A full duplicate-function sweep across the entire codebase (not just the 3 touched files) confirms zero duplicates remain anywhere.
**No functional changes** — this is a pure code-hygiene release, two rounds of adversarial review (self-check + external critique) before shipping given the "silently changes which implementation runs" risk class, even though both copies were confirmed identical in every case.

## v9.01.01 — Bug fixes for v9.01: CC ReferenceError, PI Canvas crash risk, MI Current View sections, Discovery Map modal, FC/SC/CC three-way labeling

**Critical fix — Capability Canvas ReferenceError:** `ccRenderMainContent()`'s main card-rendering path referenced `_isCap` without it being in scope at that point (only declared inside two earlier, block-scoped early-return branches) — every click on an already-generated metric threw uncaught, silently leaving the panel blank (only "All Caps," a separate code path, worked). Also explained the "doesn't retrieve generated caps on Home navigation" report — the same crash fires during automatic session-resume rendering. Fixed with the same safe inline pattern already used correctly elsewhere in the file; full audit confirmed this was the only broken instance among 7 `_isCap` usages.
**Fixed — PI tab never appearing after staging stories without opening PI Canvas first:** root-caused via diagnostic logging to `piPlan` being genuinely `null` at the moment "Send to PI" runs, for any session that reaches Story Canvas without visiting PI Canvas first — the existing guard silently skipped populating the backlog rather than initializing it. Fixed with three coordinated changes: `newScSendToPI()` now initializes a minimal `piPlan` if missing; `piOnTabEnter()`'s board-vs-empty routing now checks for a genuine generated plan (`sprints.length>0`) rather than bare truthiness, so it correctly shows the backlog-only view instead of crashing; `piRenderBoard()` itself got the same defensive guard centrally, protecting all 7 of its call sites uniformly. (Caught via adversarial review before shipping: the original minimal fix alone would have introduced a new crash in PI Canvas itself.)
**Fixed — MI Current View report:** now shows Sections 1-5 only (previously showed all 8 headers with 5-8 empty). Section 5 is now populated from `miData.capabilities` (already generated, no AI call needed) instead of the Full-Report-only `docxSections.gapMatrix`. Sections 6-8 removed entirely for Current View rather than shown as empty headers. Full Report's existing (confirmed working) 8-section behavior is unchanged.
**Fixed — Discovery Map's own "Edit Capability" modal** (separate from Capability Canvas's own modal) now reads "Edit Process Area"/"Process area name" in Process-Area mode, plus the bottom status bar now reads "N process areas" instead of "N capabilities."
**Fixed — Feature Canvas and Story Canvas left panels** now show the same "Custom Value Stage"/"Custom Process Area" relabeling as Capability Canvas (previously only fixed in CC, not FC/SC) — display-only, the underlying stage/metric grouping values used for matching are untouched. Added a three-way distinction across all three canvases and Capability Canvas's own dropdowns: Process-Area mode shows "Custom Process Area," Outcome mode shows "Custom Metric" (previously hardcoded to "Custom Process Area" regardless of approach). Also corrected one inconsistency found during this fix: a capability-reassignment dropdown had been mislabeled "Custom Value Stage" (a stage-level concept) when it should show the same metric-level label as its sibling dropdown.

## v9.01 — Terminology rename ("Process Area"/"Outcome Metrics"), PI-tab resume fix, MI Full Report fix, PI Carry-Forward removal

**Terminology:** "Capability-Based" approach renamed to "Process Area" and "Outcome-Based" to "Outcome Metrics" throughout the app (toggle, subtitle, session-card pills, settings description, tooltips) — internal value strings (`'capability-based'`/`'outcome-based'`) deliberately left unchanged. "Parent Capability" renamed to "Process Area" across traceability panels, AI prompts, and the Capability Canvas panel title/empty-states (conditional on approach, confirmed the screen is genuinely shared between both approaches). Critical fix found via adversarial review: both the xlsx and CSV capability-upload parsers matched headers by exact `"parent capability"` prefix — updated to accept both `"parent capability"` and `"process area"`, so old downloaded templates keep working after the template file's header was regenerated.
**CC left panel / Add Capability modal:** "PI Plan" → "Custom Value Stage", "Custom Capabilities" → "Custom Process Area" across all locations (nav, group headers, card labels, dropdown options). Removed the "Edit Custom Plan" button (redundant with the existing bulk-upload-via-file path) — confirmed via grep this was its only caller; the underlying shared form-rendering function it called (used by 4 other unrelated flows) was left completely untouched.
**Removed:** PI Planning's "Previous PI Carry-Forward" upload/paste section — confirmed unused/orphaned parsing functions removed cleanly; the separate story-level "carry-forward" badge feature is untouched.
**Fixed — MI Full Report generating empty sections 5-8:** root-caused to `max_tokens=8000` likely truncating a large JSON response, silently defaulting to an empty object on parse failure with no visible error. Raised to 16000, added a matching extended proxy timeout (`mi-docx-gen`, 150s) so the fix doesn't trade a truncation failure for a timeout failure, and added a lightweight schema check (required fields present and non-empty) so valid-but-incomplete AI responses now surface a visible retry error instead of silently shipping a blank report.
**Fixed — PI tab not reappearing after resuming a session with staged PI stories:** the reveal check was a bare truthiness test on `piPlan`, true even for the default empty object created at session start — now requires genuine staged content. Also fixed: sending stories to PI never emitted a live-sync content event at all (confirmed root cause for shared-session collaborators getting no refresh notification), and the action's own save is now awaited before the function returns, closing a possible race with session-navigation's own save. Temporary diagnostic logging added at both the save and resume paths to confirm the fix under real reproduction — flagged for removal once confirmed across a few real sessions.
**Found, not fixed in this build (flagged for separate follow-up):** 6 pre-existing duplicate function definitions discovered during this build's verification pass (`scPanelGenerateStories`, `buildAIRecommendationsPrompt`, `ccSyncExportBtn`, `ccRenderExportBtn`, `ccExportDocx`, `_ccGetVisibleCapsSnapshot`) — confirmed present in v9.00.01 already, not introduced by this build; out of scope here.

## v9.00.01 — AI Recommendations rerouted to the Render proxy (corporate gateway block fix)

**Root cause:** Home's AI Recommendations called `/api/anthropic` same-origin, rewritten by Netlify to a separate Netlify Function (`anthropic-proxy.js`) — distinct infrastructure from the Render proxy every other AI call in the app (`callAPI()`) uses. HCL's corporate web security gateway was blocking this specific path ("Suspicious" category, likely path/keyword-based on "anthropic" in the URL), while the long-allowlisted Render domain was unaffected — confirmed via direct testing that real generation calls succeeded on the same network while this one failed with a 403 HTML block page.
**Fix:** rerouted `home.js`'s AI Recommendations call to the same Render proxy URL (`PROXY_URL` from `env.js`, same convention `api.js` already uses) instead of the Netlify Function path. Request body/headers were already fully compatible with `server.js`'s `/api/anthropic` route (confirmed by reading its `requireActiveCompanyMember` and main handler before changing anything) — no reshaping needed.
**Also added:** a `_caller: 'ai-recommendations'` tag to the request body, so Render's own server-side logs distinguish this call type from PI Planning/other generation calls, matching the pattern already used elsewhere.
**Adversarially checked:** confirmed CORS preflight headers already allow the required headers, confirmed the new timeout budget (120s) is more generous than the old Netlify Function's tight 2s JWKS/60s function budget (not a regression), confirmed local dev (`localhost:3000`) is unaffected since it already used a separate `LOCAL_PROXY` branch untouched by this change. `netlify/functions/anthropic-proxy.js` and its `netlify.toml` rewrite rule are left in place, unused but not deleted — a separate decision.

## v9.00 — Phase 6 rename decided against; `mt_` naming made permanent (documentation-only, no code logic changes)

**Decision:** the Phase 6 "cutover" plan (renaming `mt_companies`/`mt_users_companies`/`mt_products`/`mt_company_settings`/`mt_sessions`/`mt_session_content_events` to drop the `mt_` prefix) was evaluated via two rounds of adversarial review (Postgres migration mechanics, then the corresponding frontend/proxy code diff) and decided against — `mt_` reads as "multi-tenant," a genuinely self-documenting permanent convention, and the rename carried real coordinated Render/Netlify/DB cutover risk for zero user-facing benefit.
**Spec updated:** `multi-user-rbac-spec.md`'s Phase 6 section now explicitly marks the rename sub-steps closed/decided-against, while preserving the separate, still-open question of archiving prod's existing single-tenant `sessions`/`profiles` data — that remains real, unresolved work folded into current prod-migration planning, not silently dropped alongside the rename decision.
**Code:** one stale comment in `session-store.js` corrected — previously described `mt_sessions` as temporary build/test scaffolding "flips to 'sessions' at cutover"; now states the `mt_` prefix is permanent.
**No functional changes** in this version — this is a housekeeping/documentation release. Version bumped to v9.00 to mark the close of the multi-tenant RBAC + live-sync build effort (Phases 0–5, live sync, and Phase 6 all now resolved one way or another) ahead of prod deployment planning.

## v8.151 — Repeating post-resume toast fix (sc/pc cursor-seeding omission)

**Root cause:** `_LS_ALL_CANVASES`, the array used to seed every canvas's "already caught up" cursor on resume, was never updated when Story Canvas (`sc`) and Prototype Canvas (`pc`) were added as live-sync canvases in Build B — confirmed via grep as this array's sole use site, so no other behavior (polling query, tab-visibility sync, badge counts) was affected by the gap or by this fix.
**Symptom:** after resuming a shared session, `sc`/`pc` cursors never advanced, so every subsequent ~10s poll re-flagged already-current `sc`/`pc` content as new, repeating the "has made updates" banner/toast indefinitely.
**Fix:** added `'sc'` and `'pc'` to `_LS_ALL_CANVASES`. Confirmed via code inspection that `sc`/`pc` events share the same monotonic id sequence already used to seed the other five canvases, and that the polling comparison already keys off `r.canvas` generically with no hardcoded allowlist — so this extends an existing, proven mechanism rather than introducing a new one.
**Scope:** standalone fix, unrelated to v8.149/v8.150's resume-staleness or Last Active work — different function, different call site, no shared state.

## v8.150 — Corrected resume-staleness fix and card-ordering fix, after both were found incomplete in live testing

**Resume-staleness fix, corrected (Issue 2):** the v8.149 attempt was live-tested and found not to actually fix the reported bug. Root cause of the miss: it only protected one specific call site (re-resuming the exact same session), computed via an explicit flag passed in by that one caller — but the actual destructive save happens earlier, the moment someone navigates away to Home at all (a different call site entirely), before they ever get to re-resuming anything. By the time re-resuming happened, the damage was already done and the v8.149 check was already false. Corrected by moving the detection inside the cleanup function itself, automatically, so every caller is protected uniformly — not just one that remembers to opt in. The check itself was also hardened after a flaw found before this shipped: the first design would have kept using the visible notification banner's own state as the signal, which is cleared both by a genuine apply *and* by the user simply dismissing the banner — meaning a dismissal alone would have looked like "nothing pending," silently reintroducing the bug. Uses a new, dedicated flag instead, set when the background watch discovers unapplied changes and cleared only on a genuinely successful apply, plus a check of this browser's own unflushed local edits. A related console error (`generation_lock_not_acquired`) surfacing after the same stale overwrite is consistent with this same root cause corrupting lock-related bookkeeping stored on the same record, not a separate bug.

**Card-ordering fix (Issue 3):** the "Last Active" badge and the actual first-position card in the session grid were being decided completely independently — a fix corrected which session gets the badge, but never touched the grid's own ordering (a separate sort, defaulting to "Last Modified"), so the two could point at different sessions. Now pins the badged session to the front, but only under that default sort — an explicitly chosen alternate sort (alphabetical, by name) is left completely alone, and a badged session already excluded by the current filter/search is never resurrected.

**Known, accepted residual gap in the resume-staleness fix, not further pursued:** the background watch that detects remote changes polls roughly every 10 seconds. In that narrow window between a collaborator's change landing and this browser's own next poll, the new safety check would still say "nothing pending" even though newer content already exists. Closing this fully would require making the cleanup function asynchronous, a larger structural change to a function this project treats as unusually sensitive — not taken on for a residual window this narrow, for an internal tool with a small number of concurrent users.

## v8.149 — D8 copy fix, resume staleness fix, "Last Active" now per-user

**D8 copy fix:** the Story Canvas coarse-apply description always said "updated" regardless of whether the feature was genuinely new — confirmed via live testing this was misleading for a first-time addition (e.g. sending a Diagnostics experiment to Feature Canvas). Now distinguishes "added" from "updated" based on whether the feature existed locally before the apply.

**Resuming a session can no longer entrench staleness (Issue 1):** confirmed root cause — resuming a session already correctly fetches fresh content from the database for shared sessions, but first calls the existing "leaving my session" cleanup, which unconditionally saves whatever's currently in the browser's memory back to the database. Re-resuming the *same* session you're already stale on meant that save wrote your own stale copy over a collaborator's fresher one, moments before the fresh-fetch step that was supposed to fix exactly that. Fixed narrowly: resuming the same, already-active, shared session now skips that pre-emptive save and trusts the incoming fetch completely. Every other use of the cleanup function (switching to a genuinely different session, private sessions, logout) is unaffected — confirmed via a full sweep of every call site. This required a small, explicitly sign-off-approved edit to a function this project's own rules mark as requiring separate approval before any change.

**"Last Active" is now a real per-user, per-account property (Issue 2):** previously computed by sorting every cached session by a single, global "last saved" timestamp that any collaborator's edit could bump — meaning someone else editing a shared session could silently make it show up as *your* "Last Active" session, even if you'd never opened it. Fixed with a new, durable per-person pointer (new columns on `mt_users_companies`, written via a dedicated security-definer RPC — that table has no direct UPDATE policy at all, confirmed before designing this), updated only when a person actually creates or resumes a session, and following them to any device. Everything else that already correctly used the shared global timestamp (the "Updated X ago" line, the explicit "sort by last modified" option) is untouched.

## v8.148 — Message-copy audit, Diagnostics sent-status sync (D8), D3 diagnostics

**Message-copy audit, confirmed complete across the whole file:** every Story Canvas and Prototype Canvas description was still using generic hardcoded wording ("Story Canvas updated by a teammate," "A story was updated by a teammate," "Prototype generated") — a regression of the exact fix already made once for Capability Canvas and PI in v8.144, never carried over when Build B's own descriptions were written. Every description now names the actual person (falling back to "A teammate" only if genuinely uncaptured) and the specific feature or story affected, matching the precision already established elsewhere. Swept the entire file afterward to confirm nothing else has the same gap — CC's and PI's own descriptions were already correct, only the newly-added Build B ones needed this.

**Diagnostics "sent" status now syncs (item D8):** confirmed the root cause — Diagnostics' own "N of M sent to Feature Canvas" indicator is a derived cache (`laSentIds`), rebuilt from `scCanvas` itself, not synced independently. A coarse Story Canvas apply already correctly syncs the underlying feature entry (including which Diagnostics experiment it came from), but never triggered the existing rebuild function afterward — now it does, and re-renders Diagnostics immediately if that's the tab currently in view.

**Diagnostic logging added for a still-unexplained silent-failure case (item D3):** a permanent, lightweight pair of console logs at the mark and flush points, so the next occurrence is diagnosable directly from a normal build's console.

**Deferred, per explicit decision:** the story-ordering issue (item A4) — left as-is for now, not fixed in this round.

## v8.147 — Fixes found through live testing of Build B, plus a much larger audit gap closed

**Critical infrastructure bug, root cause of nearly every "no notification" result from live testing:** `_lsEmitManualEditEventsForTargets` was built with the mark and apply halves wired up for Story Canvas and Prototype Canvas, but never actually given emit branches for those two areas — every manual edit for them was silently producing nothing at all (no error, no event, no toast), while generation events kept working since those bypass this function entirely. Fixed by adding both branches.

**Prototype wireframe decompression bug:** wireframe HTML is stored LZString-compressed in every saved snapshot and only ever decompressed inside `sessionStoreRestore()`'s own code — the live-sync apply path copied the raw, still-compressed snapshot entry directly, producing "Wireframe not available" after every live apply. Fixed by extracting the decompression logic into a shared, pure function used by both paths, so this can't drift out of sync a second time (the same lesson already learned once for tab-visibility sync).

**A much larger, previously entirely-unaudited parallel editing surface, found through live testing rather than code review:** the original audit only covered Story Canvas's own newer UI (`story-canvas-new.js`). Feature Canvas has its own, separate set of story/feature-editing functions (its own panel, triggered independently) that were never checked — 14 of 18 had no save at all, none had any live-edit marking, including direct story title/statement/AC edits, a third entry point for adding/removing whole features (the Capability Drawer's own checkbox), bulk stage-purge and bulk feature-upload actions, and a third file (`product-leak-analysis.js` / Product Diagnostics) found sending features directly into the same shared canvas. All ~18 functions fixed with the same save-then-mark chaining, coarse-vs-surgical targeting, and PI-staging-is-not-live-synced conventions already established for everything else in this build.

**Confirmed not needed, per explicit decision:** Prototype Canvas screenshot persistence — deliberately not live-synced or made to survive a hard refresh, since it's accepted as a temporary aid, not shared content.

## v8.146 — Build B: manual-edit batching and generation sync extended to Story Canvas and Prototype Canvas

**Bundled fixes, found during audit before designing this:** 8 Story Canvas manual-edit functions never called `sessionStoreSave()` at all — including one literally named `newScSaveNotes()` — and 3 Prototype Canvas functions (screenshot upload/remove, add-gap-to-context) had the identical gap. All fixed with the same save-guard used everywhere else in these files.

**Design went through a full external critique before building, given the genuinely new architecture involved** — Story Canvas is a nested structure (feature-entries each owning their own story array), unlike PI's single record or CC's one level of nesting where parent records themselves never changed. Real findings incorporated:
- A single coarse target (a whole feature-entry, insert/replace/remove based on presence in the latest snapshot) was confirmed to correctly and safely unify CC's send-to-SC/remove-from-SC actions, AI story generation, and story add — collapsing several distinct actions into one apply rule rather than building bespoke handling for each.
- Individual story edits (DoR toggle, dependency add/remove, notes, single-story reassignment) turned out to all be **surgical** (patch/insert/remove by direct story ID) once the actual code was traced — story IDs are globally unique by construction, removing the exact-match-or-abort complexity CC's name-based capability matching needed, though existence/parent-membership is still validated before patching, never assumed.
- Prototype Canvas staleness must be explicitly triggered from the apply function itself, not assumed to happen via rendering — confirmed the existing local `pcMarkStale()` call is already wired into every local story-mutation path, meaning the live-sync apply path (which bypasses those functions entirely) would silently skip it without an explicit call.
- A follow-up critique round specifically stress-tested the one accepted risk (a two-target story reassignment where one notification succeeds and the other transiently fails) — confirmed no realistic path to permanent corruption, only a narrow, self-correcting window; adopted the zero-cost mitigation of always emitting the old-parent target before the new-parent one, which the existing code's own call ordering already does for free.

**Schema:** five new event types (`stories_generated`, `story_manually_updated`, `feature_stories_manually_updated`, `prototype_generated`, `prototype_manually_updated`), plus a new RLS policy branch for the three manual-edit types, matching the 6-minute freshness window already established for CC and PI.

## v8.145 — Fix: content banner overlapped/obscured confirm dialogs
- Confirmed and re-diagnosed from an earlier finding that was never actually built: the persistent content-sync banner (z-index 9998) only ever hid at the very end of the refresh flow, so it stayed visibly overlapping any confirm dialog (z-index 600) shown during that same flow — every wholesale-canvas refresh (Discovery Map, PI, MI, Diagnostics) and CC's own edit-conflict confirm were all affected, not just the one case reported.
- Fixed by hiding the banner immediately after `_lsBannerRefreshClick` captures its needed session/event data into local variables, before any confirm can ever show — confirmed safe since the rest of the function only references those locals afterward, never the globals `_lsHideContentBanner()` clears.

## v8.144 — Two fixes found during item 2 testing
- Edit Capability modal: Cancel/Save Changes footer could vanish permanently when editing a capability with zero features (a warning-strip element it expected to swap in only exists when the capability has features). Fixed to only hide the footer when a real replacement exists.
- "Updated by a teammate" replaced with the actual person's name across all three places it appeared (CC whole-list, CC per-feature, PI's own manual-edit description), falling back to "A teammate" only if the name genuinely wasn't captured.

## v8.143 — Fix: CC manual-edit target delimiter collided with existing key formats
- The `||` delimiter used for CC's per-target dirty-tracking collided with pre-existing composite key formats already used internally (`ccPanelCapKey`'s reconstruction, `ccPIKey()`'s `'pi||'` prefix) — a metric key could legitimately already contain `||`, corrupting the intended boundary.
- Fixed by switching to the ASCII Unit Separator control character (`\u001F`), confirmed unused elsewhere in the codebase and never sent to the database. Also fixed two more pre-existing vulnerable usages found during a final check inside `_lsApplyCCEvents` itself.
- Kept a lighter, permanent version of the diagnostic logging used to find this, so a future failure of this safety check is diagnosable without a special debug build.

## v8.142 — Items 2, 3, 4 together
- **Item 4** (FK `ON DELETE` cleanup): already resolved, both FKs already `SET NULL`.
- **Item 3**: all 10 flagged em-dash toast/confirm strings reworded to plain sentences.
- **Item 2**: manual-edit batching extended to Capability Canvas, generalized from PI's single-flag design to a per-target `Set` (CC has unbounded independent capabilities, unlike PI's one plan). Two full rounds of external critique incorporated (snapshot-then-diff flush semantics, per-target success/failure tracking, canonicalization, coarse-vs-surgical targeting). Bundled fix: `ccDoRemoveCapability()` never saved at all.
- Schema: two new event types + RLS policy for CC manual edits; PI's own freshness window widened 60s→6min as a preventive fix for a latent mismatch found during this review.

## v8.141 — Item 8: manual PI edit batching/notification
- Manual edits to PI Planning (moves, point/field/note edits, dependency add/remove, remove-to-backlog) now notify collaborators in a shared session, batched rather than one notification per click.
- Design went through a full external critique before building: mark-after-save-succeeds (not before), dirty-state clears only on confirmed emit success, debounce-with-max-wait (25s idle / 5min ceiling), pause-not-clear when a real regeneration starts.
- Bundled fix: `piRemoveStoryFromSprint()` never saved at all, same class of gap as item 7.
- Schema: new `pi_plan_updated` event type.

## v8.140 — Fix: Discovery Map regeneration never actually cleared piPlan
- Confirmed via targeted debug instrumentation that the real bug was upstream of everything built across the last several versions: `generateConfirmed()`'s reset hid the PI tab but never nulled `piPlan` itself — a regenerated session's saved snapshot still contained the old plan, invisible to the regenerating user but faithfully copied to any receiving viewer.
- Fixed by adding `piPlan` and five related fields to the reset, matching `state.js`'s own declared defaults. Pre-existing gap, invisible until cross-user sync started exposing what a saved snapshot actually contains.

## v8.132–v8.139 — Build A: live sync extended to Discovery Map, PI, Market Intelligence, Diagnostics; the "item 10" tab-visibility saga
- **v8.132 (Build A):** live-sync extended from Capability Canvas to the four wholesale-replace canvases (DM, PI, MI, Diagnostics) plus rename-sync. Per-canvas cursors, per-section confirm gating, both a self- and external-critique pass run before building.
- **v8.133:** six real bugs found via live two-user testing, all fixed (missing fields in cross-user apply, a pre-confirm lock-conflict peek added app-wide, a destructive reset that ran before the lock was even checked, three PI manual-edit functions that never saved at all).
- **v8.134 (rolled back):** an attempt at tab-visibility sync introduced serious regressions; reverted in full in v8.135.
- **v8.136:** item 10 redesigned from scratch — extracted the session-resume path's "apply state" and "sync tab visibility" logic into two shared, pure functions, used by both normal resume and cross-user apply, so the two paths can no longer silently drift apart (which is exactly what caused v8.134's regression).
- **v8.137–v8.139:** three more narrow, confirmed bugs found through continued live testing (a missing render argument, a hardcoded tab-list that was structurally guaranteed to eventually miss one, CC's own apply never revealing its tab) — each fixed at the root rather than patched around.

## v8.116–v8.131 — Phase 5 (session sharing + generation lock), the live-sync rollback, and Phase 2–3 of the rebuild
- **v8.116:** Phase 5 — sessions can be shared with your team; a generation lock prevents two people running AI generation on the same session at once. A real pre-existing JS-injection bug in session-card rename/delete was found and fixed along the way.
- **v8.117–v8.119:** real bugs found via live dual-user testing of Phase 5, all fixed (a lock-rejection toast with no name, a button staying disabled after being blocked, a PI plan visually disappearing on a blocked regen attempt).
- **v8.120–v8.122 — rollback:** the live session-list/content-sync work built after Phase 5 was reverted in full after live testing found it could silently corrupt a shared session's saved data. Sharing and the generation lock themselves were unaffected and stayed shipped.
- **v8.123–v8.126:** live sync rebuilt from scratch, more conservatively — Phase 2 (content-change event emission only, nothing reads it yet) then Phase 3 (Home poll, in-session watch, content banner, resume pre-fetch, unshare kickout), each with its own self- and external-critique pass before building.
- **v8.127–v8.131:** real bugs found via live testing of the Phase 3 rebuild, all fixed (a refresh that aborted instead of adding new content, an RLS violation during unshare, a stale session card after kickout, and several render/badge sync gaps).

## v8.107–v8.115 — Phase 3–4: Settings RBAC, Team Management, real AI-generation authorization
- **v8.107:** Phase 3 — Settings sections gated by role (Regular Users get view-only or hidden sections, matching RLS rules that were already enforced at the database level but never reflected in the UI).
- **v8.109–v8.112:** Phase 4 — Team Management (invite/role/disable/delete) with three new admin-safe Postgres RPCs (row-locked to prevent a last-admin race), plus real bug fixes (a row-menu clipping issue, a `created_by` FK migration, invite-link redirect support, a genuinely broken password-setup flow found and fixed).
- **v8.113–v8.115:** found and closed a real, serious gap — `/api/anthropic`'s hosted path had no real authentication at all; rebuilt with proper JWT verification and company-membership checks on both proxy implementations. Also: zero-company UX, a non-functional boot gate restored, and further real bugs from live testing.

## v8.99–v8.106 — Phase 1–2: Auth & Signup, then moving company/product data off the single-tenant table
- **v8.99:** Phase 1 — real multi-company auth, signup, company creation, and a hard boot-gate to prevent acting before company context resolves.
- **v8.100–v8.103:** real bugs found via testing, all fixed (an RLS infinite-recursion bug, a zero-company screen removed in favor of the existing empty state, signup validation gaps, a toast-ordering and CSS-specificity bug).
- **v8.104–v8.106:** Phase 2 — company/product profile data moved off the old single-tenant `profiles` table onto real per-company tables; found and fixed a serious pre-existing gap where the boot gate had been silently non-functional since v8.102, plus a product-creation bug and a BYOK key-scoping bug.

## v8.92–v8.98 — PI Planning overhaul; KPI tree depth setting; various export fixes
- v8.98: PI generation's scoring/sequencing logic moved out of the AI prompt into deterministic code (dependency graph traversal, cycle detection, capacity-aware sequencing) — fixes timeouts on large sessions.
- v8.97/v8.96: fixed a white-border flash during wireframe capture and DOCX export (unscoped CSS leaking into the live page during capture).
- v8.92: KPI tree depth setting added (1–3 levels); PI Planning prompt/scoring overhaul (7 changes); stories/ACs per-feature caps raised.

## v8.79–v8.91 — Prototype Canvas built; document intelligence; various small fixes
- v8.79–v8.84: Prototype Canvas added (AI-generated wireframes + design brief), then hardened over several rounds of adversarial review and live-testing fixes (two-call generation split for reliability, partial-save recovery, bullet-list parsing fixes).
- v8.78: document intelligence — session/company documents now inject grounding context into FC, SC, PI, MI, and DM prompts, routed by document type.
- v8.88–v8.91: Capability Canvas state-retention fixes, a browser-autofill credential-corruption fix, and various small UI polish items (scrollbars, duplicate-name blocking, app rename to "AI PM Toolkit").

## v8.57–v8.78 — Org API keys; session documents; MI/CC/FC architecture changes
- v8.57/v8.68/v8.73: BYOK gates progressively removed in favor of an org-level API key with per-call model defaults, once the proxy could reliably fall back to it.
- v8.58: document intelligence's first version — binary file parsing (docx/pdf) fixed, LLM-based classification and summarization added at upload time, a persistent Session Documents panel added to Home.
- v8.69–v8.71: a capability-generation hang traced to a template-literal formatting bug; proxy timeout/logging added; a `callAPI()` argument-order regression fixed.
- v8.22: Market Intelligence's capability flow rearchitected to route through Capability Canvas rather than straight to Feature Canvas.

## v8.34–v8.56 — Proxy JWT hardening; session-name header rework; various UI fixes
- v8.34: proxy JWT verification moved to JWKS-based ES256, after Supabase migrated its signing keys.
- v8.39–v8.45: the session-name header went through several rounds of real, live-found bugs (text-colour, inline-edit blur handling, left-panel sync) before landing on a stable, non-destructive show/hide design.
- Various: CC/FC/SC visual and interaction-model fixes, table column-width fixes, a proxy Windows/corporate-network TLS fix.

## v8.07–v8.33 — Phase 1 of the original auth system; Home dashboard; session persistence
- v8.07: first login/signup screen and auth gate (Supabase-backed), replacing no-auth access entirely.
- v8.09–v8.25: env.js credential separation, avatar/profile UI, and — steps 5–7 — session and profile data given real Supabase persistence (write-through cache over the existing localStorage layer).
- v7.86–v8.06: the Home tab redesigned around a real session library (search/filter/sort, auto-save, inline rename) replacing the old single-session model; AI Recommendations panel added.

## v7.50–v7.85 — Settings page; capability-driven mode; demo data; PI/SC split
- v7.50–v7.56: Settings page built out — Company Profile, Product Profiles (multi-profile CRUD), file uploads with word-count limits.
- v7.57–v7.66: Home tab session launcher added (product/approach/mode selection); Capability-Driven mode added as an alternative to Outcome-Based; demo data enriched with two more product datasets.
- v7.69–v7.85: capability-driven terminology cleanup, manual capability/feature upload with fuzzy-matching mapping, Discovery Map capability edit/delete, and various selection-scoping fixes across CC/FC.
- v7.73–v7.78: an in-flight-AI-generation leave-guard added app-wide ("Hold on, don't lose this"), with one critical hotfix (v7.76) after it introduced an infinite-recursion regression in tab switching.

## Pre-v7.50 History (v2.0 – v7.35)
- **v2.0–v6.79:** original modularization from a single HTML file; Diagnostic View/Product Leak Analysis, Market Intelligence, and PI Planning tabs added one at a time; Capability Canvas built; a Modal Construction Standard and uniform card-interaction model established across all canvases.
- **v7.00:** Feature Canvas / Story Canvas architectural split — Feature Canvas keeps features and story generation, Story Canvas becomes the dedicated grooming tab.
- **v7.01–v7.33:** a long visual/interaction-standardization pass across CC/FC/SC/MI/Diagnostics/PI (group headers, left-panel and right-panel anatomy, collapse/expand behavior, spacing) culminating in a full `DESIGN_SYSTEM.md` rewrite.
- **v7.34–v7.35:** tab badge removal, auto-scroll/auto-open on manual add, Settings Save & Exit flow.

---
*Format rule: every new version entry gets a maximum of 4 bullet points. Pure bug-fix versions get a single line. Entries that are part of one continuous phase or effort are grouped under one combined header rather than kept as separate entries.*
