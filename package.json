{
  "name": "aipm-toolkit",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify Functions dependencies for AI PM Toolkit (netlify/functions/anthropic-proxy.js). Root-level only, distinct from proxy/package.json — Netlify's build bundles function dependencies declared here, not in a nested package.json.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  }
}
